---
phase: 04-protocol-documentation-drafts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - DOC-03
tags: [docs, planning-amendment, requirement-amendment]

must_haves:
  truths:
    - "REQUIREMENTS.md DOC-03 wording no longer contains the parenthetical 'email' as the sole disclosure channel"
    - "REQUIREMENTS.md DOC-03 names 'disclosure channel' (may enumerate GitHub Security Advisory / email / equivalent)"
    - "ROADMAP.md Phase 4 SC3 no longer says 'disclosure email that round-trips a live email'"
    - "ROADMAP.md Phase 4 SC3 says 'disclosure channel that round-trips a live test report (e.g., a Security Advisory receipt)'"
    - "Amendment lands in a standalone `docs(req):` commit BEFORE any SECURITY.md content commit"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Amended DOC-03 wording for disclosure channel"
      contains: "disclosure channel"
    - path: ".planning/ROADMAP.md"
      provides: "Amended Phase 4 SC3 wording"
      contains: "Security Advisory receipt"
  key_links:
    - from: ".planning/REQUIREMENTS.md DOC-03"
      to: ".planning/ROADMAP.md Phase 4 SC3"
      via: "matching 'disclosure channel' language"
      pattern: "disclosure channel"
    - from: "git commit"
      to: "subsequent SECURITY.md commit"
      via: "sequencing — amendment must be its own commit landing first"
      pattern: "docs\\(req\\): amend DOC-03"
---

<objective>
Amend REQUIREMENTS.md DOC-03 and ROADMAP.md Phase 4 SC3 to replace "email" language with "disclosure channel" language, resolving the literal-text conflict between the existing requirement and the chosen GitHub Security Advisory disclosure channel.

Purpose: This is a PRE-WRITE BLOCKER for Phase 4. Per D-SEC-04, SECURITY.md cannot be written until the requirement's email wording is amended, because otherwise the verifier flags a literal-text contradiction between the old DOC-03 ("disclosure email") and the new SECURITY.md ("GitHub Advisory only").
Output: Two amended files and one standalone `docs(req):` commit. No new files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-protocol-documentation-drafts/04-CONTEXT.md
@.planning/phases/04-protocol-documentation-drafts/04-RESEARCH.md
@.planning/phases/04-protocol-documentation-drafts/04-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Amend REQUIREMENTS.md DOC-03 and ROADMAP.md Phase 4 SC3 for disclosure-channel language</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (line 123 is the DOC-03 row to amend; verify its current text matches the expected pre-amendment string before editing)
    - .planning/ROADMAP.md (line 80 is Phase 4 SC3; verify its current text matches the expected pre-amendment string before editing)
    - .planning/phases/04-protocol-documentation-drafts/04-CONTEXT.md (D-SEC-04 is the amendment authority; exact replacement text is specified there)
    - .planning/phases/04-protocol-documentation-drafts/04-RESEARCH.md (§Code Examples "REQUIREMENTS.md amendment (D-SEC-04)" block has the exact before/after text)
  </read_first>
  <action>
    Make exactly two text replacements, then commit.

    **Edit 1 — `.planning/REQUIREMENTS.md` line 123:**

    Replace the entire line:
    ```
    - [ ] **DOC-03**: `SECURITY.md` has a working disclosure contact (email) and a 90-day embargo policy statement
    ```
    with:
    ```
    - [ ] **DOC-03**: `SECURITY.md` has a working disclosure channel (GitHub Security Advisory, email, or equivalent) and a 90-day embargo policy statement
    ```

    **Edit 2 — `.planning/ROADMAP.md` line 80:**

    The current line reads (verify before editing):
    ```
      3. `SECURITY.md` exists at repo root with a disclosure email that round-trips a live email (verified by a committed note of the test), a stated 90-day embargo policy, and a reference to the cclink lineage including the `cipherpost/v1` HKDF info prefix that matches the constants module from Phase 1.
    ```

    Replace with:
    ```
      3. `SECURITY.md` exists at repo root with a disclosure channel that round-trips a live test report (e.g., a Security Advisory receipt) (verified by a committed note of the test), a stated 90-day embargo policy, and a reference to the cclink lineage including the `cipherpost/v1` HKDF info prefix that matches the constants module from Phase 1.
    ```

    Preserve all surrounding whitespace, list indentation, and line-ending style exactly as in the source files. Do NOT renumber other list items. Do NOT touch any other DOC-* row in REQUIREMENTS.md or any other SC row in ROADMAP.md. Do NOT edit the Traceability table at the bottom of REQUIREMENTS.md (DOC-03's status remains "Pending").

    **Commit (standalone — must be the only substantive change in this commit):**
    ```
    docs(req): amend DOC-03 and Phase 4 SC3 to say disclosure channel

    Replace "email" wording in REQUIREMENTS.md DOC-03 and ROADMAP.md
    Phase 4 SC3 with "disclosure channel" language, matching the
    GitHub Security Advisory channel chosen in 04-CONTEXT.md D-SEC-01
    (and D-SEC-04 mandating this pre-write amendment).

    Resolves the literal-text conflict so Phase 4 SECURITY.md can land
    without verifier contradiction.
    ```

    Include only `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md` in the commit — no other files.
  </action>
  <verify>
    <automated>bash -c 'grep -n "disclosure channel (GitHub Security Advisory, email, or equivalent)" .planning/REQUIREMENTS.md && grep -n "Security Advisory receipt" .planning/ROADMAP.md && ! grep -n "working disclosure contact (email)" .planning/REQUIREMENTS.md && ! grep -n "disclosure email that round-trips a live email" .planning/ROADMAP.md && git log -1 --pretty=%s | grep -E "^docs\(req\): amend DOC-03 and Phase 4 SC3"'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "disclosure channel (GitHub Security Advisory, email, or equivalent)" .planning/REQUIREMENTS.md` returns `1`
    - `grep -c "working disclosure contact (email)" .planning/REQUIREMENTS.md` returns `0`
    - `grep -c "Security Advisory receipt" .planning/ROADMAP.md` returns `1`
    - `grep -c "disclosure email that round-trips a live email" .planning/ROADMAP.md` returns `0`
    - `grep -c "DOC-03" .planning/REQUIREMENTS.md` is unchanged from pre-amendment count (still appears in both the row and the Traceability table; only the row text changed)
    - `git log -1 --pretty=%s` matches regex `^docs\(req\): amend DOC-03 and Phase 4 SC3`
    - `git log -1 --stat --name-only` shows exactly two files changed: `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md`
    - No other `Phase 4 SC*` or `DOC-*` lines in REQUIREMENTS.md/ROADMAP.md are changed
  </acceptance_criteria>
  <done>
    - Both files amended
    - Standalone `docs(req): amend DOC-03 and Phase 4 SC3 to say disclosure channel` commit lands
    - Commit contains exactly the two files listed in `files_modified`
    - Pre-write blocker (D-SEC-04) cleared for Plan 04-04 (SECURITY.md) to proceed
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git history → downstream plans | Plans 04-02/03/04/05 read REQUIREMENTS.md/ROADMAP.md as inputs; amendment landing out-of-order poisons their source-of-truth |
| planning docs → verifier | Phase-close verifier cross-references plan frontmatter `requirements:` against REQUIREMENTS.md IDs; unexpected edits break that cross-reference |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Tampering | REQUIREMENTS.md rows unrelated to DOC-03 | mitigate | `git log -1 --stat` acceptance criterion confirms only two files changed; diff review discipline — edit only lines 123 and 80 (ROADMAP) |
| T-04-01-02 | Tampering | Traceability table at bottom of REQUIREMENTS.md | mitigate | Acceptance criterion inspects that DOC-03's Traceability row ("Phase 4 | Pending") is preserved unchanged — amendment modifies only the DOC-03 descriptive row on line 123 |
| T-04-01-03 | Repudiation | Amendment commit conflated with content commit | mitigate | Commit message pattern enforced by verify regex `^docs\(req\): amend DOC-03 and Phase 4 SC3`; commit-scope acceptance criterion asserts exactly two files in the commit |
| T-04-01-04 | Information disclosure | None (planning-doc edits contain no secrets) | accept | N/A — both files are public-by-design planning artifacts |
| T-04-01-05 | Denial of service (plan-sequencing) | Wave 1 amendment missed, SECURITY.md committed first | mitigate | Plan 04-04 declares `depends_on: [04-01, 04-02]` in frontmatter; wave scheduler enforces amendment commits land before SECURITY.md content executes. Task 1 Step A provides a runtime double-check via `git log \| grep "docs(req): amend DOC-03"` before any content write. Defense-in-depth; if either wave scheduling or the runtime check is bypassed, the other still holds. |
| T-04-01-06 | Elevation of privilege | None | accept | No privilege boundary traversed |
</threat_model>

<verification>
The amendment is "done" when:
1. Both files show only the two intended line changes (no incidental edits, no whitespace churn).
2. The commit is atomic (`git log -1 --stat` shows exactly `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md`).
3. The commit message matches the `docs(req): amend DOC-03 and Phase 4 SC3` pattern.
4. Grep confirms the new wording present AND the old wording absent (both directions verified — adding without removing would leave a residual contradiction).
</verification>

<success_criteria>
Plan 04-01 is complete when all acceptance criteria on Task 1 pass. The downstream plans may now read the amended requirement text as source-of-truth for DOC-03 wording. No further edits to REQUIREMENTS.md or ROADMAP.md in Phase 4 — subsequent DOC-03 completion is marked via post-phase transition workflow, not in-phase editing.
</success_criteria>

<output>
After completion, create `.planning/phases/04-protocol-documentation-drafts/04-01-SUMMARY.md` documenting:
- Exact line numbers amended (may differ from 123 / 80 if files drifted — verify at execution time)
- Commit SHA of the `docs(req):` commit
- Confirmation that downstream plans (04-02, 04-03, 04-04, 04-05) are now cleared to write content
</output>
</output>
