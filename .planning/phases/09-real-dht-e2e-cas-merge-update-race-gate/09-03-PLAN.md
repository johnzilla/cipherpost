---
phase: 09-real-dht-e2e-cas-merge-update-race-gate
plan: 03
type: execute
wave: 3
depends_on:
  - 09-01
  - 09-02
files_modified:
  - RELEASE-CHECKLIST.md
  - RELEASE-CHECKLIST-v1.1.md
  - README.md
  - SPEC.md
  - CLAUDE.md
  - .planning/STATE.md
autonomous: true
requirements:
  - DHT-06
tags:
  - docs
  - release-checklist
  - claude-md
  - spec
  - state-md
  - phase-closure

must_haves:
  truths:
    - "RELEASE-CHECKLIST.md exists at repo root as the living template (~80 lines per D-P9-C1; markdown checkboxes per D-P9-C3)"
    - "RELEASE-CHECKLIST-v1.1.md exists at repo root as the v1.1 versioned snapshot (D-P9-C4 — committed at Phase 9 close per Discretion recommendation)"
    - "RELEASE-CHECKLIST manual real-DHT command uses cargo-nextest invocation (NOT the never-existed `cargo --test-timeout` flag — 09-RESEARCH.md OQ-5 correction over CONTEXT.md `<specifics>` mockup)"
    - "README.md gains a single-sentence note that v1.1 uses pkarr default Mainline bootstrap nodes (D-P9-B2 + Discretion: single sentence, NOT a new section)"
    - "SPEC.md gains three inline additions: bootstrap-defaults note in §3 Wire Format; CAS contract note in §3 (single-retry-then-fail receivers MUST implement); §Pitfall #22 extension recording the pin+burn+~2KB GenericSecret composite Phase 9 measurement"
    - "CLAUDE.md §Load-bearing lock-ins gains exactly THREE new bullet entries: (1) CAS retry contract; (2) bootstrap defaults; (3) real-DHT cfg-flag discipline"
    - ".planning/STATE.md \"Verify pkarr 5.0.4 ClientBuilder bootstrap configurability at Phase 9 plan time\" todo is closed with a resolution note citing D-P9-B1 + 09-RESEARCH.md OQ-4"
    - "Zero source code edits in this plan; zero new tests; zero new fixtures (matches Phase 8 Plan 06 'phase-closure docs plan' precedent)"
  artifacts:
    - path: RELEASE-CHECKLIST.md
      provides: "Living template — copied per release into RELEASE-CHECKLIST-vX.Y.md"
      contains:
        - "[ ] cargo nextest run"
        - "--features real-dht-e2e"
        - "--run-ignored only"
        - "filter-expr 'test(real_dht_e2e)'"
        - "192 bytes"
        - "424 bytes"
        - "119 bytes"
        - "212 bytes"
        - "142 bytes"
    - path: RELEASE-CHECKLIST-v1.1.md
      provides: "First versioned snapshot — committed unticked at Phase 9 close; ticked at v1.1 release tag time"
      contains:
        - "Cipherpost v1.1 Release Checklist"
        - "Tag:** v1.1.0"
    - path: README.md
      provides: "Single-sentence bootstrap-defaults note (D-P9-B2)"
      contains:
        - "pkarr default"
    - path: SPEC.md
      provides: "Three inline additions (bootstrap defaults; CAS contract note; Pitfall #22 composite extension)"
      contains:
        - "single-retry"
        - "ConcurrencyError"
    - path: CLAUDE.md
      provides: "Three new Load-bearing lock-in entries"
      contains:
        - "single-retry-then-fail"
        - "CIPHERPOST_DHT_BOOTSTRAP"
        - "real-dht-e2e"
    - path: .planning/STATE.md
      provides: "Bootstrap-configurability todo closed with D-P9-B1 + OQ-4 citation"
      contains:
        - "D-P9-B1"
  key_links:
    - from: "RELEASE-CHECKLIST.md §Manual real-DHT gate"
      to: "tests/real_dht_e2e.rs"
      via: "cargo nextest run --features real-dht-e2e invocation matching .config/nextest.toml profile"
      pattern: "cargo nextest run --features real-dht-e2e"
    - from: "CLAUDE.md §Load-bearing lock-ins"
      to: "src/transport.rs CAS retry path + Cargo.toml real-dht-e2e feature"
      via: "three new bullets explaining the contract additions"
      pattern: "single-retry-then-fail|real-dht-e2e"
    - from: "SPEC.md §Pitfall #22"
      to: "Plan 09-01 09-01-SUMMARY.md (encoded byte count for pin+burn+2KB composite)"
      via: "Phase 9 measurement row in the per-variant wire-budget table"
      pattern: "pin.*burn.*GenericSecret"
---

<objective>
DHT-06 — close the v1.1 milestone with the documentation that gates every
v1.1+ release. Phase 9 is otherwise feature-complete after Plans 09-01 and
09-02; this plan is the "phase-closure docs plan" pattern established by
Phase 8 Plan 06: zero source code, zero new tests, zero new fixtures —
purely repo-root + planning-corpus updates that codify the decisions and
artefacts of the preceding two plans.

Purpose: closes DHT-06 (the last of 7 Phase 9 requirements). Establishes a
living `RELEASE-CHECKLIST.md` template + a versioned `RELEASE-CHECKLIST-v1.1.md`
snapshot at repo root; the template gates every v1.1+ release per D-P9-C2/C3/C4.
The CLAUDE.md lock-in additions ensure the three new contracts (CAS retry,
bootstrap defaults, real-DHT cfg-flag discipline) are encoded in the
"don't break this" source-of-truth that future phases consult before
touching transport or test infrastructure. The STATE.md todo closure marks
the Phase 9 deliverable cycle complete.

CRITICAL CORRECTION over CONTEXT.md: the CONTEXT.md `<specifics>`
RELEASE-CHECKLIST mockup at line 327 says
`cargo test --features real-dht-e2e -- --ignored --test-timeout 120 dht_e2e`
— **that command is wrong on stable cargo** (09-RESEARCH.md OQ-5: `cargo
--test-timeout` does not exist). Replace with the cargo-nextest invocation
that pairs with `.config/nextest.toml`'s `[[profile.default.overrides]]
slow-timeout` (added by Plan 09-02).

Output:
- `RELEASE-CHECKLIST.md` (NEW, ~80 lines, repo root) — living template, markdown checkboxes.
- `RELEASE-CHECKLIST-v1.1.md` (NEW, ~80 lines, repo root) — v1.1 versioned snapshot, header customised, body unticked.
- `README.md` (modified — single-sentence bootstrap note).
- `SPEC.md` (modified — three inline additions: §3 bootstrap inline; §3 CAS contract note; §Pitfall #22 composite extension).
- `CLAUDE.md` (modified — three new Load-bearing lock-in bullet entries).
- `.planning/STATE.md` (modified — close bootstrap-configurability todo with D-P9-B1 + OQ-4 citation).

`autonomous: true` because everything in this plan is text-only and runs
through the standard repo lints (lychee link-check, fmt-check on touched
markdown). Zero source code edits.

Wave 2 because:
1. The RELEASE-CHECKLIST manual nextest invocation references the exact
   feature flag + filter Plan 09-02 finalises.
2. The SPEC.md §Pitfall #22 composite measurement row needs Plan 09-01's
   `09-01-SUMMARY.md` to record the actual `encoded` byte count from the
   pin+burn+2KB DHT-07 test (so the docs cite real numbers, not estimates).
3. The CLAUDE.md "single-retry-then-fail CAS contract" lock-in describes
   the code shape Plan 09-01 ships.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-real-dht-e2e-cas-merge-update-race-gate/09-CONTEXT.md
@.planning/phases/09-real-dht-e2e-cas-merge-update-race-gate/09-RESEARCH.md
@.planning/phases/09-real-dht-e2e-cas-merge-update-race-gate/09-PATTERNS.md
@.planning/phases/09-real-dht-e2e-cas-merge-update-race-gate/09-01-PLAN.md
@.planning/phases/09-real-dht-e2e-cas-merge-update-race-gate/09-02-PLAN.md
@.planning/research/PITFALLS.md
@CLAUDE.md
@README.md
@SPEC.md
@SECURITY.md

<interfaces>
From CONTEXT.md `<canonical_refs>` Phase 9 fixture byte-counts (regression guard targets for RELEASE-CHECKLIST):
- `tests/fixtures/outer_record_signable.bin` — 192 bytes (Phase 1)
- `tests/fixtures/receipt_signable.bin` — 424 bytes (Phase 3)
- `tests/fixtures/envelope_jcs_generic_secret.bin` — 119 bytes (assumed; verify against repo)
- `tests/fixtures/outer_record_pin_required_signable.bin` — 212 bytes (Phase 8 Plan 02)
- `tests/fixtures/envelope_burn_signable.bin` — 142 bytes (Phase 8 Plan 04)

From 09-RESEARCH.md OQ-5 (the corrected nextest manual invocation):
```
cargo nextest run --features real-dht-e2e --run-ignored only --filter-expr 'test(real_dht_e2e)' --no-fail-fast
```
(NOT `cargo test --features real-dht-e2e -- --ignored --test-timeout 120 dht_e2e` — that flag does not exist on stable cargo.)

From CLAUDE.md §Load-bearing lock-ins (existing entries to mirror style — bullet starts with `- ` and ends with sentence; multiple sentences allowed):
- "Canonical JSON = RFC 8785 (JCS) via serde_canonical_json (shipped as 1.0.0...)"
- "HKDF info strings = cipherpost/v1/<context>. Never empty, never None."
- "Argon2id params live in the identity file header (PHC string)..."
- "chacha20poly1305 usage only via age — no direct calls allowed."
- "Async runtime: none at the cipherpost layer. Use pkarr::ClientBlocking; no direct tokio dep."
- "serial_test = "3" + #[serial] on any test that mutates process env..."

From 09-PATTERNS.md §"CLAUDE.md (modified — three new load-bearing lock-ins)" (lines 443-450):
The three new lock-ins, with exact wording recommendations.

From SECURITY.md:1-8 (top-of-file banner pattern for repo-root markdown):
```markdown
# Security Policy

> **Status: DRAFT — skeleton milestone**
> ...
```
RELEASE-CHECKLIST adapts this to a "living template" banner.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create RELEASE-CHECKLIST.md (~80 lines living template) and RELEASE-CHECKLIST-v1.1.md (versioned snapshot)</name>
  <files>RELEASE-CHECKLIST.md, RELEASE-CHECKLIST-v1.1.md</files>
  <read_first>
    - SECURITY.md (lines 1-30 — top-of-file banner pattern + draft-status header style)
    - .planning/phases/09-real-dht-e2e-cas-merge-update-race-gate/09-CONTEXT.md `<specifics>` lines 291-343 (the ~50-line mockup the executor extends to ~80 lines per D-P9-C1)
    - .planning/phases/09-real-dht-e2e-cas-merge-update-race-gate/09-PATTERNS.md §"RELEASE-CHECKLIST.md (NEW, repo root; top-level-docs template)" (lines 266-292)
    - .planning/phases/09-real-dht-e2e-cas-merge-update-race-gate/09-PATTERNS.md §"RELEASE-CHECKLIST-v1.1.md (NEW, repo root; first versioned snapshot)" (lines 294-310)
    - .planning/phases/09-real-dht-e2e-cas-merge-update-race-gate/09-RESEARCH.md §"OQ-5" (lines 249-276 — the corrected nextest manual invocation)
    - .planning/phases/09-real-dht-e2e-cas-merge-update-race-gate/09-01-PLAN.md (read after Plan 09-01 EXECUTES — the SUMMARY will record the actual `encoded` byte count for the pin+burn+2KB composite; if 09-01-SUMMARY.md exists, use the recorded number; if not yet written, leave a `<TBD: from 09-01-SUMMARY>` placeholder and the executor MUST fill it after running this task's verify)
    - .github/workflows/ci.yml (the CI invocation pattern — RELEASE-CHECKLIST cites the same gates CI runs, plus the manual-only real-DHT step)
    - deny.toml (cargo deny policy — RELEASE-CHECKLIST lists `cargo deny check` as a gate)
  </read_first>
  <action>
**EDIT A — Create `RELEASE-CHECKLIST.md` at repo root.** Per D-P9-C1 (~80
lines), D-P9-C3 (markdown checkboxes), D-P9-C2 (lives at `/RELEASE-CHECKLIST.md`,
NOT `/docs/`):

```markdown
# Cipherpost Release Checklist (template)

> **Status: living template — copied per release into `RELEASE-CHECKLIST-vX.Y.md`**
>
> This template is the source-of-truth for every v1.1+ release. At ship
> time, `cp RELEASE-CHECKLIST.md RELEASE-CHECKLIST-v<version>.md`, fill in
> the release-specific fields, and tick each box as the gate is verified.
> The ticked snapshot ships in the release commit; the template stays
> unticked.

**Release date:** YYYY-MM-DD
**Releaser:** @<github-username>
**Tag:** v<version>

___

## Pre-flight

- [ ] All Phase VERIFICATION.md files for this milestone signed off (pass)
- [ ] PROJECT.md §Validated reflects shipped scope; §Active is empty for the closing milestone
- [ ] Cargo.toml `version =` matches the release tag
- [ ] CHANGELOG entry drafted (if a CHANGELOG exists; not required for v1.1)

## Code gates (run from repo root)

- [ ] `cargo fmt --check` — no diff
- [ ] `cargo clippy --all-targets --all-features -- -D warnings` — clean
- [ ] `cargo audit` — no advisories OR documented exceptions in `deny.toml`
- [ ] `cargo deny check` — clean
- [ ] `cargo test` (no features) — all green
- [ ] `cargo test --features mock` — all green (≥309 tests for v1.1; bumps with each milestone)
- [ ] `cargo test --doc` — doctests green
- [ ] `lychee --offline SPEC.md THREAT-MODEL.md SECURITY.md README.md CLAUDE.md RELEASE-CHECKLIST.md` — no broken links

## Wire-format byte-count regression guard

These fixtures are JCS-canonical bytes — any drift is a protocol-version-bump
event. Run `wc -c tests/fixtures/<name>.bin` for each:

- [ ] `tests/fixtures/outer_record_signable.bin` — **192 bytes** (Phase 1; OuterRecord)
- [ ] `tests/fixtures/receipt_signable.bin` — **424 bytes** (Phase 3; Receipt)
- [ ] `tests/fixtures/envelope_jcs_generic_secret.bin` — **119 bytes** (Phase 1/2; Envelope generic-secret)
- [ ] `tests/fixtures/outer_record_pin_required_signable.bin` — **212 bytes** (Phase 8 Plan 02; OuterRecord with pin_required=true)
- [ ] `tests/fixtures/envelope_burn_signable.bin` — **142 bytes** (Phase 8 Plan 04; Envelope with burn_after_read=true)

## Manual real-DHT gate (DHT-03/04/05)

This is the cipherpost-specific release-acceptance step that does NOT run in
CI (D-P9-D2 + Pitfall #29). Allow ~10s between consecutive runs for socket
teardown (09-RESEARCH.md Pitfall C).

- [ ] Confirm running on a network with normal outbound UDP egress (corporate
      / restrictive firewalls block Mainline DHT bootstrap; the test will
      skip with `"real-dht-e2e: UDP unreachable; test skipped (not counted
      as pass)"` — re-run from a permissive network if observed)
- [ ] Run: `cargo nextest run --features real-dht-e2e --run-ignored only --filter-expr 'test(real_dht_e2e)' --no-fail-fast`
      (the `slow-timeout = { period = "60s", terminate-after = 2 }` profile
      override in `.config/nextest.toml` enforces a 120s wall-clock cap)
- [ ] Test passes within 120s OR skips with the canonical UDP-unreachable
      message (skip is not a release blocker; rerun on a permissive network)
- [ ] Output observation: round trip completes; receipt count == 1 under
      bob's z32 (BURN-04 invariant)

## Security review

- [ ] Review RUSTSEC advisory list for any new advisories against current
      crate deps since the previous release; document any new acceptances
      in `deny.toml`
- [ ] Confirm SECURITY.md disclosure channel is live (link to GitHub Security
      Advisory page resolves)
- [ ] Verify `chacha20poly1305` does NOT appear as a direct dep:
      `cargo tree | grep -E "^chacha20poly1305" || echo "PASS: chacha20poly1305 only via age"`
- [ ] HKDF info-enumeration test green:
      `cargo test --test hkdf_info_enumeration` — every call-site uses
      `cipherpost/v1/<context>` prefix (CLAUDE.md load-bearing)
- [ ] Debug-leak scan green:
      `cargo test --test debug_leak_scan` — no `format!("{:?}", x)` on key
      bytes contains the seed material

## Release artifacts

- [ ] Create git tag `v<version>` on the release commit
- [ ] Update MILESTONES.md with the milestone close summary (one paragraph
      per milestone; cite VERIFICATION.md for the proof)
- [ ] Snapshot this file as `RELEASE-CHECKLIST-v<version>.md` (ticked) at
      the release commit. The template stays unticked at `RELEASE-CHECKLIST.md`.

___

*Template lifecycle: living document. Versioned snapshots are committed at
release time per D-P9-C4.*
```

**EDIT B — Create `RELEASE-CHECKLIST-v1.1.md` at repo root.** Per D-P9-C4
(versioned snapshot at Phase 9 close per Discretion recommendation, NOT
deferred to v1.1 release tag time):

Body identical to `RELEASE-CHECKLIST.md` template above, with TWO header
differences:
1. The top-of-file banner is replaced (drop "living template" framing) with:
   ```markdown
   # Cipherpost v1.1 Release Checklist

   **Release date:** YYYY-MM-DD (filled at v1.1 release tag time)
   **Releaser:** @<github-username>
   **Tag:** v1.1.0

   ---
   ```
2. Boxes remain unticked (`[ ]`); ticking happens at v1.1 release time per
   D-P9-C4.

Drop the closing "Template lifecycle" sentence and replace it with:
```markdown
*Snapshot of `RELEASE-CHECKLIST.md` at v1.1 close (committed unticked at
Phase 9 close per D-P9-C4 + Discretion recommendation). Ticking happens
at v1.1 release tag time.*
```

Everything else (Pre-flight / Code gates / Wire-format byte-counts /
Manual real-DHT gate / Security review / Release artifacts) is identical
to the template.

**Note on the SPEC.md §Pitfall #22 composite measurement:** if Plan 09-01's
`09-01-SUMMARY.md` records the encoded byte count for the pin+burn+2KB
composite, the Wire-format section MAY be augmented with a 6th bullet
naming that test's encoded byte count as a regression guard. If the SUMMARY
does not yet exist (executor running Plan 09-03 sees neither file), DO NOT
add a placeholder — the existing 5 fixtures are sufficient for v1.1; future
milestones can add new fixture rows as needed.
  </action>
  <verify>
    <automated>cd /home/john/vault/projects/github.com/cipherpost &amp;&amp; ls -la RELEASE-CHECKLIST.md RELEASE-CHECKLIST-v1.1.md &amp;&amp; wc -l RELEASE-CHECKLIST.md RELEASE-CHECKLIST-v1.1.md &amp;&amp; grep -cE '^- \[ \]' RELEASE-CHECKLIST.md &amp;&amp; lychee --offline RELEASE-CHECKLIST.md RELEASE-CHECKLIST-v1.1.md 2>&amp;1 | tail -10</automated>
  </verify>
  <done>
    - `RELEASE-CHECKLIST.md` exists at repo root with line count between 60 and 100 (~80 target per D-P9-C1)
    - `RELEASE-CHECKLIST-v1.1.md` exists at repo root with similar line count
    - Both files contain ≥ 15 markdown checkboxes (`grep -cE '^- \[ \]' RELEASE-CHECKLIST.md` ≥ 15)
    - `grep -n "cargo nextest run --features real-dht-e2e" RELEASE-CHECKLIST.md` returns ≥ 1 match (corrected manual invocation per OQ-5)
    - `grep -n "cargo --test-timeout" RELEASE-CHECKLIST.md` returns 0 matches (the never-existed flag is NOT in the document)
    - `grep -nE "192 bytes|424 bytes|119 bytes|212 bytes|142 bytes" RELEASE-CHECKLIST.md` returns 5 matches (one per fixture)
    - `grep -n "real-dht-e2e: UDP unreachable; test skipped (not counted as pass)" RELEASE-CHECKLIST.md` returns ≥ 1 match
    - `grep -n "Tag:\*\* v1.1.0\|Tag:\*\* v1\\.1\\.0" RELEASE-CHECKLIST-v1.1.md` returns ≥ 1 match (versioned snapshot identifies the tag)
    - `lychee --offline RELEASE-CHECKLIST.md RELEASE-CHECKLIST-v1.1.md` reports 0 broken links (or skips gracefully if lychee not installed; in that case `grep -nE "https?://" RELEASE-CHECKLIST.md | head` returns 0 unrelated URLs that would fail in CI lychee step)
    - Both files pass `markdownlint` informally (no syntax errors; valid frontmatter not required for top-level docs)
    - `[[ "$(grep -cE '^- \[ \]' RELEASE-CHECKLIST.md)" == "$(grep -cE '^- \[ \]' RELEASE-CHECKLIST-v1.1.md)" ]] && echo "PASS"` (Fix #5: D-P9-C4 mandates the v1.1 snapshot body matches the template body verbatim — checkbox count parity is the canary for unintended drift)
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update README.md (single sentence) + SPEC.md (three inline additions) + CLAUDE.md (three Load-bearing lock-ins) + STATE.md (close bootstrap-configurability todo)</name>
  <files>README.md, SPEC.md, CLAUDE.md, .planning/STATE.md</files>
  <read_first>
    - README.md (the WHOLE file — to find the right insertion point for the bootstrap-defaults sentence; recommended location is the same paragraph as line 22's "No tokio dependency..." one-liner)
    - SPEC.md (the WHOLE file — to find §3 Wire Format and the Pitfall #22 reference; the executor inserts three inline additions, NOT new sections)
    - CLAUDE.md (lines 80-180 — the §"Load-bearing lock-ins" bullet list; the executor appends three new bullets in the existing prose style)
    - .planning/STATE.md (lines 170-180 — the "Pending Todos" section containing "Verify pkarr 5.0.4 ClientBuilder bootstrap configurability at Phase 9 plan time"; the executor marks it closed with the standard `~~strikethrough~~` + closure-citation pattern matching the existing "cclink pin/burn survey" closure)
    - .planning/phases/09-real-dht-e2e-cas-merge-update-race-gate/09-PATTERNS.md §"README.md (modified — single-sentence bootstrap note)" (lines 415-425)
    - .planning/phases/09-real-dht-e2e-cas-merge-update-race-gate/09-PATTERNS.md §"SPEC.md (modified — three inline additions)" (lines 429-440)
    - .planning/phases/09-real-dht-e2e-cas-merge-update-race-gate/09-PATTERNS.md §"CLAUDE.md (modified — three new load-bearing lock-ins)" (lines 443-450)
    - .planning/phases/09-real-dht-e2e-cas-merge-update-race-gate/09-PATTERNS.md §".planning/STATE.md (modified — close bootstrap-configurability todo)" (lines 453-458)
    - .planning/phases/09-real-dht-e2e-cas-merge-update-race-gate/09-RESEARCH.md §"OQ-1" (lines 115-157 — pkarr ConcurrencyError variants for the SPEC.md §3 CAS contract note)
    - .planning/phases/09-real-dht-e2e-cas-merge-update-race-gate/09-RESEARCH.md §"OQ-4" (lines 232-247 — pkarr ClientBuilder defaults; the closure citation for the STATE.md todo)
    - .planning/phases/09-real-dht-e2e-cas-merge-update-race-gate/09-01-PLAN.md (the must_haves + threat_model — the CLAUDE.md CAS-retry lock-in must accurately reflect what Plan 09-01 ships)
    - .planning/phases/09-real-dht-e2e-cas-merge-update-race-gate/09-02-PLAN.md (the must_haves — the CLAUDE.md real-DHT cfg-flag lock-in must accurately reflect what Plan 09-02 ships)
  </read_first>
  <action>
**EDIT A — Update `README.md`.** Per D-P9-B2 + Discretion: SINGLE SENTENCE,
NOT a new section. Insert into the same paragraph as the existing
`README.md:22` "No tokio dependency..." one-liner:

Search for the existing line:
```
Requires Rust 1.85+ (pinned in `rust-toolchain.toml`). No `tokio` dependency at the cipherpost layer — uses `pkarr::ClientBlocking`.
```

Replace with (same paragraph, two sentences now):
```
Requires Rust 1.85+ (pinned in `rust-toolchain.toml`). No `tokio` dependency at the cipherpost layer — uses `pkarr::ClientBlocking`. Bootstrap nodes are pkarr defaults (Mainline DHT — `router.bittorrent.com:6881` and three peers); no user-tunable bootstrap configuration in v1.1.
```

That is the entire README change.

**EDIT B — Update `SPEC.md`.** Per D-P9-B2 + 09-PATTERNS.md "three inline
additions, NOT new sections":

1. **§3 Wire Format — bootstrap-defaults inline note.** Search for the §3
   header in SPEC.md (e.g., `## 3. Wire Format` or `## §3 Wire formats`),
   add a one-sentence paragraph before the first subsection:
   ```markdown
   v1.1 uses the pkarr default Mainline bootstrap node set
   (`router.bittorrent.com:6881`, `dht.transmissionbt.com:6881`,
   `dht.libtorrent.org:25401`, `relay.pkarr.org:6881`); no user-tunable
   bootstrap configuration is exposed in this milestone.
   ```

2. **§3 Wire Format — CAS contract note.** Add a second one-sentence
   paragraph (or sub-paragraph under the receipt-publish discussion if §3
   has one):
   ```markdown
   `cas` semantics on `publish_receipt` are contractual: implementations
   MUST single-retry-then-fail on `pkarr::errors::ConcurrencyError`
   (`ConflictRisk` / `NotMostRecent` / `CasFailed` — all three are
   conflict-class signals per pkarr 5.0.4). Final-conflict failures
   surface via `Error::Transport` (no public `Error::CasConflict`
   variant — error-oracle hygiene per Pitfall #16). Divergence requires
   a `protocol_version` bump.
   ```

3. **§Pitfall #22 — composite extension.** Search for the Pitfall #22
   reference (per-variant wire-budget table). Add a row OR an inline
   sentence at the bottom of the existing per-variant treatment:
   ```markdown
   **Phase 9 composite measurement (DHT-07):** `pin_required=true` +
   `burn_after_read=true` + `Material::GenericSecret { bytes: vec![0u8; 2048] }`
   exceeds the 1000-byte BEP44 ceiling and surfaces
   `Error::WireBudgetExceeded { encoded, budget: 1000, plaintext }`
   cleanly at send time. Test:
   `tests/wire_budget_compose_pin_burn_pgp.rs::pin_burn_realistic_payload_surfaces_wire_budget_exceeded`.
   ```

   Read `09-01-SUMMARY.md` (mandatory; this plan is wave 3 with
   `depends_on: [09-01, 09-02]`, so the SUMMARY MUST exist by the time
   this task runs). Substitute the actual encoded byte count recorded in
   the SUMMARY directly into the SPEC.md addition (e.g., `encoded ≈ 1640
   bytes` or `encoded = 1640 bytes` — copy the exact wording the SUMMARY
   uses). If the SUMMARY for some reason omitted the count (regression
   in Plan 09-01's <output> contract), STOP this task and surface the
   omission to the orchestrator instead of falling back to a placeholder
   formula.

**EDIT C — Update `CLAUDE.md` §Load-bearing lock-ins.** Append THREE new
bullets at the END of the existing bullet list (find the last bullet
ending with `nextest parallel runner will race otherwise.` per the file
header; append after that):

```markdown
- Single-retry-then-fail CAS contract on `publish_receipt`. Both `MockTransport` and `DhtTransport` implement the contract; the retry loop lives **inside** the `Transport` trait method (caller never sees `CasConflict`). `MockTransport` models CAS via per-key `seq: u64` (matches `pkarr::Timestamp` semantics behaviorally). All three `pkarr::errors::ConcurrencyError` variants (`ConflictRisk`, `NotMostRecent`, `CasFailed`) are treated as the conflict signal. Final-conflict failures collapse into `Error::Transport` — **no public `Error::CasConflict` variant** (error-oracle hygiene; Pitfall #16). CAS-retry events log to stderr only when `CIPHERPOST_DEBUG=1` (default-silent). Tests: `tests/cas_racer.rs` exercises the trait-internal retry under Barrier-synced contention; do not relax `#[serial]`.
- No `CIPHERPOST_DHT_BOOTSTRAP` env var in v1.1 — pkarr defaults only (4 Mainline hosts: `router.bittorrent.com:6881`, `dht.transmissionbt.com:6881`, `dht.libtorrent.org:25401`, `relay.pkarr.org:6881`). `pkarr::ClientBuilder::bootstrap` exists (verified `pkarr-5.0.4/src/client/builder.rs:164`) but is intentionally NOT exercised; revisiting requires a v1.2+ milestone gate.
- Real-DHT tests behind `#[cfg(feature = "real-dht-e2e")]` + `#[ignore]` + `#[serial]`; CI never runs `--features real-dht-e2e`. The test file is `tests/real_dht_e2e.rs` (single test, single file per Pitfall #29). Outer guard via `.config/nextest.toml` `slow-timeout = { period = "60s", terminate-after = 2 }` paired with an in-test `Instant::now() >= deadline` check at 120s. RELEASE-CHECKLIST.md manual invocation is the only gate (D-P9-D2). Stable cargo has no `--test-timeout` flag — use `cargo nextest run --features real-dht-e2e --run-ignored only --filter-expr 'test(real_dht_e2e)'`.
```

**EDIT D — Update `.planning/STATE.md`.** Find the "Pending Todos" entry:
```
- Verify pkarr 5.0.4 ClientBuilder bootstrap configurability at Phase 9 plan time
```

Replace with the strikethrough+closure pattern matching the existing
`cclink pin/burn survey` closure on the line above:
```
- ~~Verify pkarr 5.0.4 ClientBuilder bootstrap configurability at Phase 9 plan time~~ (closed by D-P9-B1: pkarr defaults only for v1.1; bootstrap IS configurable via `ClientBuilder::bootstrap` per `pkarr-5.0.4/src/client/builder.rs:164` — verified in 09-RESEARCH.md OQ-4 — but the API is not exercised in v1.1. Revisit at v1.2+ if private-testnet support is requested.)
```
  </action>
  <verify>
    <automated>cd /home/john/vault/projects/github.com/cipherpost &amp;&amp; grep -n "pkarr default" README.md &amp;&amp; grep -nE "single-retry|ConcurrencyError|router\\.bittorrent\\.com" SPEC.md &amp;&amp; grep -nE "single-retry-then-fail|CIPHERPOST_DHT_BOOTSTRAP|real-dht-e2e" CLAUDE.md &amp;&amp; grep -n "D-P9-B1" .planning/STATE.md &amp;&amp; lychee --offline README.md SPEC.md CLAUDE.md 2>&amp;1 | tail -10 &amp;&amp; cargo build --features mock 2>&amp;1 | tail -3</automated>
  </verify>
  <done>
    - `grep -n "pkarr default" README.md` returns ≥ 1 match (single-sentence bootstrap note added)
    - `grep -n "router.bittorrent.com" README.md` returns ≥ 1 match
    - `grep -n "tokio" README.md` continues to return ≥ 1 match (existing line preserved)
    - `grep -n "single-retry" SPEC.md` returns ≥ 1 match (CAS contract note added)
    - `grep -n "ConcurrencyError" SPEC.md` returns ≥ 1 match (CAS contract note cites variants)
    - `grep -n "router.bittorrent.com" SPEC.md` returns ≥ 1 match (bootstrap defaults inline)
    - `grep -nE "Pitfall #22|pin.*burn.*GenericSecret|wire_budget_compose_pin_burn_pgp" SPEC.md` returns ≥ 1 match (composite measurement extension)
    - `grep -n "single-retry-then-fail" CLAUDE.md` returns ≥ 1 match (Lock-in 1)
    - `grep -n "CIPHERPOST_DHT_BOOTSTRAP" CLAUDE.md` returns ≥ 1 match (Lock-in 2)
    - `grep -n "real-dht-e2e" CLAUDE.md` returns ≥ 1 match (Lock-in 3)
    - `grep -nE "D-P9-B1" .planning/STATE.md` returns ≥ 1 match (todo closure recorded)
    - `grep -nE '~~Verify pkarr.*~~' .planning/STATE.md` returns ≥ 1 match (strikethrough applied)
    - `lychee --offline README.md SPEC.md CLAUDE.md` reports 0 broken links (lychee CI step stays green)
    - `cargo build --features mock` exits 0 — confirms zero source code edits regressed the crate
    - `cargo test --features mock` baseline + Plan 09-01 deltas remain green (test-count unchanged from Plan 09-02 verify; this plan ships zero new tests)
    - The number of new bullets added to CLAUDE.md §Load-bearing lock-ins equals exactly THREE (`grep -cE "^- " CLAUDE.md` should be exactly +3 vs. baseline; the three new bullets are at the END of the existing list, not interspersed)
    - `grep -nE "encoded ≈ [0-9]+ bytes|encoded = [0-9]+ bytes" SPEC.md` returns ≥ 1 match (Fix #2: the actual byte count from 09-01-SUMMARY.md is substituted into SPEC.md §Pitfall #22 — NOT a placeholder formula)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo-root markdown ↔ release-acceptance gate | RELEASE-CHECKLIST.md is the source-of-truth for what gates a release. Stale or omitted gates risk shipping a build that didn't pass `cargo audit`, `cargo deny check`, or the manual real-DHT step. |
| CLAUDE.md ↔ future agent decisions | Lock-ins encoded in CLAUDE.md are the "don't break this" contract for future phases. Wrong wording (e.g., "single-retry" without "then-fail") could mislead a future agent into adding unbounded retry loops. |
| RELEASE-CHECKLIST manual command ↔ tests/real_dht_e2e.rs | If the manual invocation in RELEASE-CHECKLIST drifts from the actual nextest profile filter, the test silently does not run. Mitigation: both files cite `'test(real_dht_e2e)'` as the filter expression — kept in sync by Phase 9 Plan 02 + Plan 03. |
| .planning/STATE.md todo state ↔ planning hygiene | An open todo for "verify pkarr 5.0.4 ClientBuilder bootstrap configurability" with no closure note becomes architectural debt across milestones. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03-01 | Tampering | RELEASE-CHECKLIST manual command drift | mitigate | The exact `cargo nextest run --features real-dht-e2e --run-ignored only --filter-expr 'test(real_dht_e2e)' --no-fail-fast` invocation is recorded both in RELEASE-CHECKLIST.md AND in CLAUDE.md §Load-bearing lock-ins. Drift in one would surface in the lychee link-check or, in practice, by the manual releaser noticing the test doesn't run. |
| T-09-03-02 | Information Disclosure | RELEASE-CHECKLIST documents test command syntax | accept | Documenting how to run the real-DHT test is the explicit purpose of the file (DHT-06). The command publishes ephemeral test identities to public Mainline DHT — same observability as Plan 09-02's threat-model already accepted. |
| T-09-03-03 | Repudiation | Pitfall #22 composite measurement omitted from SPEC.md | mitigate | Plan 09-01's SUMMARY records the actual `encoded` byte count for the pin+burn+2KB composite. SPEC.md §Pitfall #22 cites the test name `tests/wire_budget_compose_pin_burn_pgp.rs::pin_burn_realistic_payload_surfaces_wire_budget_exceeded` — future readers can re-run to verify. |
| T-09-03-04 | Tampering | CLAUDE.md lock-in wording inconsistent with shipped code | mitigate | The three new bullets are written AFTER Plans 09-01 and 09-02 ship (Wave 2 dependency). Each bullet cites the file (`tests/cas_racer.rs`, `tests/real_dht_e2e.rs`, `.config/nextest.toml`) the lock-in describes, anchoring the wording to actual artefacts. |
| T-09-03-05 | Information Disclosure | Bootstrap-host enumeration in CLAUDE.md / README / SPEC | accept | The four hosts (`router.bittorrent.com`, `dht.transmissionbt.com`, `dht.libtorrent.org`, `relay.pkarr.org`) are pkarr 5.0.4 + mainline 6.1.1 published defaults; documenting them is informational, not a leak. |
| T-09-03-06 | Repudiation | STATE.md todo closure without citation | mitigate | The closure note cites D-P9-B1 + 09-RESEARCH.md OQ-4 verbatim, providing an audit trail back to the source verifications. |

</threat_model>

<verification>
- `cargo build --features mock` exits 0 — zero source code edits regress the crate.
- `cargo test --features mock` baseline (309 + 2 from Plan 09-01 = 311 expected) green; this plan adds no new tests.
- `cargo fmt --check` exits 0 (no source edits; markdown is not fmt-checked).
- `lychee --offline RELEASE-CHECKLIST.md RELEASE-CHECKLIST-v1.1.md README.md SPEC.md CLAUDE.md THREAT-MODEL.md SECURITY.md` reports 0 broken links (CI lychee step protected).
- `RELEASE-CHECKLIST.md` exists at repo root with 60-100 lines, ≥ 15 markdown checkboxes, the corrected nextest invocation, the 5 fixture byte-counts, the canonical UDP-skip message string verbatim.
- `RELEASE-CHECKLIST-v1.1.md` exists at repo root as a snapshot of the template with v1.1-specific header.
- `README.md` has a single-sentence bootstrap-defaults note with the four pkarr hosts.
- `SPEC.md` has three inline additions: bootstrap-defaults note, CAS contract note, Pitfall #22 composite extension.
- `CLAUDE.md` §Load-bearing lock-ins has exactly THREE new bullets at the end (CAS retry contract; bootstrap defaults; real-DHT cfg-flag discipline).
- `.planning/STATE.md` Pending Todos has the strikethrough+closure pattern applied to the bootstrap-configurability todo, citing D-P9-B1.
</verification>

<success_criteria>
1. `RELEASE-CHECKLIST.md` (template, ~80 lines) and `RELEASE-CHECKLIST-v1.1.md` (snapshot) exist at repo root with markdown checkboxes per D-P9-C1/C2/C3/C4.
2. The RELEASE-CHECKLIST manual real-DHT command is the cargo-nextest invocation (NOT `cargo --test-timeout` per OQ-5 correction over CONTEXT.md `<specifics>`).
3. `README.md` gains a single-sentence bootstrap-defaults note (D-P9-B2 + Discretion: single sentence, NOT a new section).
4. `SPEC.md` gains three inline additions: bootstrap-defaults note in §3 Wire Format; CAS contract note (single-retry-then-fail; `ConcurrencyError` variants; no public `Error::CasConflict`); §Pitfall #22 composite extension citing the pin+burn+2KB DHT-07 test.
5. `CLAUDE.md` §Load-bearing lock-ins gains exactly THREE new bullets at the end of the existing list.
6. `.planning/STATE.md` Pending Todos has the bootstrap-configurability todo closed with `~~strikethrough~~` + D-P9-B1 + OQ-4 citation.
7. Zero source code edits; zero new tests; zero new fixtures (matches Phase 8 Plan 06 docs-only phase-closure pattern).
8. `lychee` link-check passes on all touched markdown files.
9. The combined effect of Plans 09-01, 09-02, 09-03 is that ALL 7 Phase 9 requirements (DHT-01..07) are covered: 09-01 → DHT-01/02/07; 09-02 → DHT-03/04/05; 09-03 → DHT-06.
</success_criteria>

<output>
After completion, create `.planning/phases/09-real-dht-e2e-cas-merge-update-race-gate/09-03-SUMMARY.md` capturing:
- Final byte-count line count of `RELEASE-CHECKLIST.md` and `RELEASE-CHECKLIST-v1.1.md` (target ~80 lines per D-P9-C1).
- Whether the SPEC.md §Pitfall #22 composite measurement cites a recorded `encoded` byte count from 09-01-SUMMARY.md OR the formula `encoded > budget=1000` (the latter is acceptable if Plan 09-01 ran and recorded but not yet propagated; flag for follow-up).
- Confirmation that the three CLAUDE.md bullets are at the END of the existing §Load-bearing lock-ins list (not interspersed).
- lychee output (PASS / number of skipped links).
- Confirm Phase 9 closure: list all 7 DHT requirements (DHT-01..07) and which plan covers each.
- Confirm "phase-closure docs plan" pattern (Phase 8 Plan 06 precedent): zero source code edits, zero new tests, zero new fixtures.
</output>
