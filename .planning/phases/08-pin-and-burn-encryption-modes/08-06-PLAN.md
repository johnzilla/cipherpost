---
phase: 08-pin-and-burn-encryption-modes
plan: 06
type: execute
wave: 6
depends_on: [01, 02, 03, 04, 05]
files_modified:
  - THREAT-MODEL.md
  - SPEC.md
  - CLAUDE.md
files_created: []
autonomous: true
requirements: [PIN-10, BURN-08]
tags: [docs, threat-model, spec, claude-md, consolidation, lineage]
must_haves:
  truths:
    - "THREAT-MODEL.md gains §X.Y 'PIN mode (second-factor share encryption)' subsection per PIN-10: documents (1) what the property is — both identity passphrase AND PIN required to decrypt; (2) attacker capabilities it covers — passive DHT observer + identity-key compromise alone (without PIN); (3) attacker capabilities it does NOT cover — receiver-side malware that can read both passphrase + PIN, multi-machine coordination is out-of-scope; (4) test references in tests/pin_roundtrip.rs + tests/pin_error_oracle.rs + tests/pin_burn_compose.rs"
    - "THREAT-MODEL.md §X.Y 'PIN mode' documents Argon2id offline-brute-force bound: 64 MiB × 3 iterations + 8-char minimum + anti-pattern blocklist establishes the brute-force resistance floor; PIN entropy is the user's responsibility above the floor"
    - "THREAT-MODEL.md §X.Y 'PIN mode' documents intentional indistinguishability: wrong-PIN, wrong-passphrase, and tampered-inner-ciphertext all surface as Error::DecryptFailed (exit 4) with identical Display 'wrong passphrase or identity decryption failed' — no PIN logging anywhere (no stderr, no Debug, no panic messages)"
    - "THREAT-MODEL.md gains §X.Y 'Burn mode (local-state-only single-consume)' subsection per BURN-08: (1) what the property is — receiver-side single-consumption guard; (2) attacker capabilities it covers — accidental same-machine re-decryption + UX warning before commit; (3) attacker capabilities it does NOT cover — multi-machine race (different machines with fresh ledgers can both decrypt), DHT-survives-TTL, removal of the ciphertext from PKARR is impossible without identity passphrase; (4) test references in tests/burn_roundtrip.rs + tests/state_ledger.rs + tests/pin_burn_compose.rs"
    - "THREAT-MODEL.md §X.Y 'Burn mode' explicitly describes the multi-machine race: 'Two receivers with fresh ledgers (different CIPHERPOST_HOME paths) can both successfully decrypt the same burn share until the share's TTL expires. Burn is a UX affordance, NOT a cryptographic destruction primitive. The DHT ciphertext cannot be force-deleted.'"
    - "THREAT-MODEL.md §X.Y 'Burn mode' explicitly states burn ≠ cryptographic destruction; cross-references SPEC.md §3.7 for wire-shape and Plan 04's PITFALLS.md #26 supersession"
    - "SPEC.md gets a final consolidation pass: cross-references between §3.6 PIN Crypto Stack and §3.7 Burn Semantics; §5.1 Send (--pin + --burn flags); §5.2 Receive banner [BURN] marker + PIN prompt order; §6 Exit Codes (4 unified credential failures + 7 share-already-consumed); §Pitfall #22 (wire-budget; pin+burn+typed-material composite; Phase 9 DHT-07 measurement)"
    - "CLAUDE.md §Load-bearing lock-ins gains THREE new bullets per Plan 06 (RESEARCH §'CLAUDE.md exact insertion targets'): (1) HKDF info `cipherpost/v1/pin` added to the enumeration, (2) accepted.jsonl rows carry `state: \"accepted\"|\"burned\"` field with v1.0 default-deserialization preserved (D-P8-10), (3) Burn write order is emit-before-mark per D-P8-12 — inverts v1.0 accepted-then-mark for burn ONLY"
    - "SPEC.md and THREAT-MODEL.md cross-reference cleanly — every cross-link resolves under lychee link-check"
    - "No new SPEC.md sections beyond consolidation; no new test files; no source code changes"
    - "Phase 8 closure: every REQ-ID PIN-01..10 + BURN-01..09 has its primary REQ-claim plan + a SUMMARY entry; Plan 06 SUMMARY records the closure"
  artifacts:
    - path: THREAT-MODEL.md
      provides: "§X.Y PIN mode + §X.Y Burn mode subsections — full attacker-capabilities model + test references + multi-machine race caveat for burn"
      contains: "PIN mode"
    - path: SPEC.md
      provides: "Consolidation pass: cross-refs between §3.6 + §3.7 + §5.1 + §5.2 + §6 + §Pitfall #22"
      contains: "cipherpost/v1/pin"
    - path: CLAUDE.md
      provides: "§Load-bearing lock-ins: 3 new bullets per Plan 06 final"
      contains: "cipherpost/v1/pin"
  key_links:
    - from: THREAT-MODEL.md §PIN mode
      to: SPEC.md §3.6 PIN Crypto Stack
      via: "cross-ref for wire-shape, KDF params, error-oracle constraint"
      pattern: "SPEC.md.*3.6"
    - from: THREAT-MODEL.md §Burn mode
      to: SPEC.md §3.7 Burn Semantics + .planning/research/PITFALLS.md #26
      via: "cross-ref for ledger inversion, emit-before-mark, supersession of pre-Phase-8 mark-then-emit reasoning"
      pattern: "SPEC.md.*3.7"
    - from: CLAUDE.md §Load-bearing lock-ins
      to: src/crypto.rs::hkdf_infos::PIN constant + src/flow.rs::LedgerEntry.state field + src/flow.rs::run_receive emit-before-mark contract
      via: "the lock-ins point at the load-bearing code anchors via prose; future readers checking CLAUDE.md see the contract before touching the code"
      pattern: "cipherpost/v1/pin|state.*accepted.*burned|emit-before-mark"
---

<objective>
Land the Phase 8 documentation consolidation. PIN-10 + BURN-08 (the two THREAT-MODEL.md REQ-IDs) covered exclusively here. SPEC.md cross-reference cleanup; CLAUDE.md load-bearing lock-ins extended.

This plan ships ZERO code. It is the closure plan for Phase 8.

1. **THREAT-MODEL.md §X.Y PIN mode** (PIN-10) — new subsection following the existing per-property template (e.g., §3.1 Sybil, §3.3 Replay format). Documents:
   - **What the property is:** PIN is a second-factor layered via nested age encryption. Both the receiver's identity passphrase AND the PIN are required to decrypt.
   - **Threat coverage:** passive DHT observer cannot decrypt; identity-key compromise alone (without PIN) cannot decrypt; the PIN entropy floor (8-char + anti-pattern blocklist) + Argon2id 64 MiB × 3 iter establishes the offline brute-force bound.
   - **Threats NOT covered:** receiver-side malware that captures both passphrase and PIN at the keyboard (PIN is a second factor, not a magical defense against compromised endpoints); multi-machine coordination (PIN-protected shares can be received from any machine that has both the identity file and the PIN — out-of-scope for v1.x).
   - **Indistinguishability invariant:** wrong-PIN, wrong-passphrase, tampered-inner-ciphertext all surface as Error::DecryptFailed (exit 4) with identical Display. NO PIN logging anywhere — not stderr, not Debug, not panic messages.
   - **Test references:** tests/pin_roundtrip.rs (PIN-08 matrix), tests/pin_error_oracle.rs (Display equality), tests/pin_burn_compose.rs (cross-cutting), tests/debug_leak_scan.rs (PIN bytes redacted).
   - **Cross-refs:** SPEC.md §3.6 (wire-shape + KDF params); PITFALLS.md #23/#24 (the original PIN distinguishability research).

2. **THREAT-MODEL.md §X.Y Burn mode** (BURN-08) — new subsection immediately after PIN mode. Documents:
   - **What the property is:** Receiver-side single-consumption guard. After the first successful receive, the local ledger records `state: "burned"` and any subsequent receive against the same share_ref returns exit 7 ("share already consumed").
   - **Threat coverage:** prevents accidental same-machine re-decryption (the user has multiple shells/scripts pointing at the same ciphertext); UX warning surface (BURN-05 stderr + banner [BURN] marker) gives the user awareness before committing.
   - **Threats NOT covered:**
     * **Multi-machine race:** two receivers with fresh ledgers (different `CIPHERPOST_HOME` paths) can both successfully decrypt the same burn share until TTL expires. Burn is a UX affordance, NOT a cryptographic destruction primitive.
     * **DHT-survives-TTL:** the encrypted payload remains in the DHT until the TTL (24h default) expires. Cipherpost cannot force-delete published ciphertext (the DHT is not under our control).
     * **Cryptographic erasure:** burn ≠ key destruction. The ciphertext can still be decrypted by any party with the receiver's identity passphrase + PIN (if pin_required) at any time before TTL expiry.
   - **Test references:** tests/burn_roundtrip.rs (BURN-09), tests/state_ledger.rs (schema migration), tests/pin_burn_compose.rs (cross-cutting + negative-path safety).
   - **Cross-refs:** SPEC.md §3.7 (wire-shape + emit-before-mark); .planning/research/PITFALLS.md #26 (SUPERSEDED by D-P8-12 — preserves the rejected "mark-then-emit" alternative as historical context).

3. **SPEC.md final consolidation pass** — no NEW sections (those landed in Plan 02 §3.6, Plan 04 §3.7). This is cross-reference cleanup:
   - §3.6 PIN Crypto Stack: add cross-refs to THREAT-MODEL.md §PIN mode + Pitfall #22 wire-budget context
   - §3.7 Burn Semantics: add cross-refs to THREAT-MODEL.md §Burn mode + PITFALLS.md #26 supersession
   - §5.1 Send: ensure --pin and --burn flags reference §3.6 / §3.7
   - §5.2 Receive: ensure PIN prompt order + [BURN] marker placement reference §3.6 / §3.7
   - §6 Exit Codes: ensure exit 4 (DecryptFailed unified across credential failures) and exit 7 (Declined / share-already-consumed) reference the relevant subsections
   - §Pitfall #22 (wire-budget): mention pin+burn+typed-material composite size measurement deferred to Phase 9 DHT-07; cross-ref RESEARCH.md Open Risk #5 + Plan 05's wire-budget pre-flight test

4. **CLAUDE.md §Load-bearing lock-ins** — append three new bullets at appropriate positions per RESEARCH §"CLAUDE.md exact insertion targets":
   - **(a) After the existing "HKDF info strings" bullet** — append a sub-clause: "Phase 8 adds `cipherpost/v1/pin` to the enumeration; the PIN-derived 32-byte X25519 scalar is wrapped into an age::Identity for nested encryption — `chacha20poly1305 only via age` invariant unchanged."
   - **(b) After the "ed25519-dalek =3.0.0-pre.5 exact pin" bullet** — add a NEW bullet: "`accepted.jsonl` rows carry an optional `state: \"accepted\"|\"burned\"` field; v1.0 rows missing the field deserialize via serde default to `accepted`. Burn rows write `state: \"burned\"` (D-P8-10). Schema migration is wire-format-additive — v1.0 receivers parse v1.1 rows by ignoring the new field."
   - **(c) After the "Error-oracle hygiene" bullet** — add a NEW bullet: "Burn write order is **emit-before-mark** (D-P8-12), inverting v1.0's accepted-then-emit ordering for burn shares ONLY. Crash between emit and ledger-write leaves share re-receivable — safer failure mode than losing user data to a crashed state-write. v1.0 accepted flow is unchanged. PITFALLS.md #26 superseded; original analysis preserved with a SUPERSEDED header."

Out-of-scope for Plan 06:
- Source code changes (Phase 8 code is fully shipped after Plan 05)
- New tests (Plan 05 closes the integration-test surface)
- Phase 9 DHT-07 wire-budget measurement (separate phase)
- v1.2 escape-hatch design (out-of-milestone)

Purpose: Phase 8 closure. The two THREAT-MODEL.md REQ-IDs (PIN-10 + BURN-08) cannot ship until the docs land; this plan ships them. SPEC.md gets one consolidation pass to ensure cross-references resolve. CLAUDE.md gets the three load-bearing lock-ins so future Claude instances see the Phase 8 contracts before touching the code.

Output: Phase 8 DONE. All 19 REQ-IDs covered: PIN-01..10 (Plans 01-02 + Plan 06's PIN-10 prose) + BURN-01..09 (Plans 03-05 + Plan 06's BURN-08 prose). `lychee SPEC.md THREAT-MODEL.md SECURITY.md README.md` exit 0. STATE.md updated to Phase 9 ready.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08-pin-and-burn-encryption-modes/08-CONTEXT.md
@.planning/phases/08-pin-and-burn-encryption-modes/08-RESEARCH.md
@.planning/phases/08-pin-and-burn-encryption-modes/08-PATTERNS.md
@.planning/phases/08-pin-and-burn-encryption-modes/08-01-PLAN.md
@.planning/phases/08-pin-and-burn-encryption-modes/08-02-PLAN.md
@.planning/phases/08-pin-and-burn-encryption-modes/08-03-PLAN.md
@.planning/phases/08-pin-and-burn-encryption-modes/08-04-PLAN.md
@.planning/phases/08-pin-and-burn-encryption-modes/08-05-PLAN.md
@CLAUDE.md
@SPEC.md
@THREAT-MODEL.md
@.planning/research/PITFALLS.md

<interfaces>
From Plans 01-05 (landed):
- src/pin.rs: pin_derive_key + validate_pin + prompt_pin (Plans 01-02)
- src/crypto.rs::hkdf_infos::PIN constant (Plan 01)
- src/record.rs::OuterRecord.pin_required + Envelope.burn_after_read (Plan 01)
- src/lib.rs::is_false helper (Plan 01)
- src/flow.rs::LedgerState enum + check_already_consumed + LedgerEntry.state field (Plan 03)
- src/flow.rs::Prompter::marker param + run_receive nested-decrypt + emit-before-mark for burn (Plans 02-04)
- src/cli.rs::Send.pin + Send.burn flags (Plans 02-03)
- SPEC.md §3.6 PIN Crypto Stack (Plan 02 Task 5)
- SPEC.md §3.7 Burn Semantics (Plan 04 Task 4)
- .planning/research/PITFALLS.md #26 SUPERSEDED header (Plan 04 Task 4)
- All test files (Plans 01-05)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: THREAT-MODEL.md adds §X.Y PIN mode and §X.Y Burn mode subsections</name>
  <files>THREAT-MODEL.md</files>
  <read_first>
    - THREAT-MODEL.md full (existing section structure: §1 Trust Model → §2 Identity → §3 DHT Adversaries → §4 Sender-Purpose → §5 Acceptance-UX → §6 Passphrase-MITM → §7 Receipt-Replay/Race → §8 Out of Scope → §9 Lineage)
    - .planning/phases/08-pin-and-burn-encryption-modes/08-RESEARCH.md §"THREAT-MODEL.md insertion points" (recommend §6.5 + §6.6 — no renumber needed; alternative §7 + §8 with renumber)
    - .planning/phases/08-pin-and-burn-encryption-modes/08-CONTEXT.md §canonical_refs §"Spec sections to edit"
    - .planning/phases/08-pin-and-burn-encryption-modes/08-PATTERNS.md §"Plan 06 (docs) anchors" (THREAT-MODEL.md insertion details)
  </read_first>
  <behavior>
    - THREAT-MODEL.md gains §6.5 'PIN mode (second-factor share encryption)' AFTER existing §6 Passphrase-MITM (no renumber needed; appends as §6.5)
    - THREAT-MODEL.md gains §6.6 'Burn mode (local-state-only single-consume)' AFTER §6.5 (no renumber)
    - Each new section follows the existing per-property template: (1) what the property is, (2) attacker capabilities it covers, (3) attacker capabilities it does NOT cover, (4) test references in tests/, (5) cross-references to SPEC.md / PITFALLS.md
    - PIN mode prose explicitly states: indistinguishability invariant (wrong-PIN ≡ wrong-passphrase ≡ tampered-inner Display + exit 4); no PIN logging anywhere; offline brute-force bound (Argon2id 64 MiB × 3 iter + 8-char floor + anti-pattern blocklist)
    - Burn mode prose explicitly states: multi-machine race caveat (different CIPHERPOST_HOME paths can both decrypt until TTL); DHT-survives-TTL caveat; burn ≠ cryptographic destruction; receipt IS published on burn-receive (not suppressed)
    - Both sections list test references that exist on disk (tests/pin_roundtrip.rs, tests/pin_error_oracle.rs, tests/burn_roundtrip.rs, tests/state_ledger.rs, tests/pin_burn_compose.rs, tests/debug_leak_scan.rs)
    - lychee link-check passes after edit (no broken cross-refs)
  </behavior>
  <action>
    **Step A — Locate insertion point.**

    ```bash
    grep -nE '^##? ' THREAT-MODEL.md | head -20
    ```

    Find §6 Passphrase-MITM. Plan 06 inserts the two new subsections AFTER its body ends (or as §6.5 and §6.6 if §6 is split into subsections; otherwise as §7 PIN mode + §8 Burn mode and renumber subsequent sections).

    Recommend using §6.5 and §6.6 — no renumber required, less diff churn. (If THREAT-MODEL.md doesn't already use sub-numbering, escalate to §7 + §8 with renumber of the existing trailing sections.)

    **Step B — Insert §6.5 PIN mode (or whatever section number is appropriate).**

    Required content:

    ```markdown
    ## §6.5 PIN mode (second-factor share encryption)

    *Phase 8 — REQ PIN-10. See SPEC.md §3.6 for wire-shape and KDF parameters.*

    ### Property

    PIN-protected shares (`OuterRecord.pin_required = true`) require BOTH the
    receiver's identity passphrase AND a PIN to decrypt. The PIN is layered via
    nested age encryption (`age_encrypt(envelope_jcs, pin_recipient)` then
    `age_encrypt(inner_ct, receiver_recipient)`); the 32-byte salt is embedded at
    the front of the b64-decoded blob. Both keys are required; possession of one
    without the other yields no decryption.

    ### Threat coverage

    - **Passive DHT observer** cannot decrypt the share. The blob is doubly age-
      encrypted; even with the salt visible (intentional — see SPEC.md §3.6),
      the attacker must still derive the PIN-recipient via Argon2id (64 MiB × 3
      iterations) AND obtain the receiver's identity passphrase.
    - **Identity-key compromise alone** (e.g., stolen `~/.cipherpost/identity`
      file + breach of the receiver's identity passphrase) does NOT enable
      decryption of PIN-protected shares. The attacker still needs the PIN.
    - **Offline brute-force bound:** the PIN entropy floor (8-char minimum + the
      anti-pattern blocklist preventing all-same / sequential / common 8-char
      breach-corpus entries) combined with Argon2id 64 MiB × 3 iter establishes
      the bound on offline brute-force feasibility. PIN entropy ABOVE the floor
      is the user's responsibility.

    ### Threats NOT covered

    - **Receiver-side endpoint compromise.** A compromised receiver machine
      (keylogger, screen-capture, malicious shell history) can capture both the
      passphrase AND the PIN at user input time. PIN is a second factor — not a
      magical defense against a compromised endpoint.
    - **Multi-machine coordination.** A PIN-protected share can be decrypted
      from any machine that has both the identity file AND the PIN. The PIN is
      not bound to a specific receiver-machine; this is intentional (PIN flows
      out-of-band, not via PKARR), but means a leaked PIN + leaked identity-file
      bypass on any machine.
    - **DHT-side share revocation.** PIN-protected ciphertext remains on the DHT
      until TTL expires (24h default). Cipherpost cannot force-delete published
      shares.

    ### Indistinguishability invariant

    Wrong-PIN, wrong-passphrase, and tampered-inner-ciphertext ALL produce
    `Error::DecryptFailed` with the IDENTICAL user-facing Display
    (`"wrong passphrase or identity decryption failed"`) and exit code 4. No
    PIN bytes appear in any user-facing output — not stderr, not Debug, not
    panic messages. This prevents the attacker who can run the receive command
    from learning which credential was wrong.

    Sig-failures (`Error::Signature*`, exit 3) remain a DIFFERENT lane; an
    attacker observing exit code can distinguish credential-failure (4) from
    sig-failure (3) but cannot distinguish wrong-PIN from wrong-passphrase.

    ### Test references

    - `tests/pin_roundtrip.rs::pin_self_round_trip_recovers_plaintext` — PIN-08(a) correct PIN
    - `tests/pin_roundtrip.rs::pin_wrong_pin_returns_decrypt_failed` — PIN-08(b) wrong PIN
    - `tests/pin_error_oracle.rs::wrong_pin_display_matches_wrong_passphrase_display` — PIN-07 indistinguishability
    - `tests/pin_burn_compose.rs::wrong_pin_on_pin_burn_share_does_not_mark_burned_and_share_remains_re_receivable` — wrong-PIN safety property
    - `tests/debug_leak_scan.rs::pin_secret_box_debug_redacts` — leak-scan invariant
    - `tests/hkdf_info_enumeration.rs` — `cipherpost/v1/pin` namespace invariant

    ### Cross-references

    - SPEC.md §3.6 PIN Crypto Stack — wire-shape, KDF parameters, salt encoding,
      receive-flow ordering, error-oracle constraint, entropy floor algorithm
    - PITFALLS.md #23 — PIN-as-distinguishable-oracle (mitigation enforced here)
    - PITFALLS.md #24 — PIN entropy floor (algorithm landed in src/pin.rs::validate_pin)
    ```

    **Step C — Insert §6.6 Burn mode immediately after §6.5.**

    Required content:

    ```markdown
    ## §6.6 Burn mode (local-state-only single-consume)

    *Phase 8 — REQ BURN-08. See SPEC.md §3.7 for wire-shape and ledger ordering.*

    ### Property

    Burn shares (`Envelope.burn_after_read = true`) are single-consumption from
    the receiver's perspective. After a successful first receive, the local
    ledger records `state: "burned"` and any subsequent receive against the
    same `share_ref` returns exit 7 (`Error::Declined`) with stderr message
    `"share already consumed (burned at <timestamp>)"`. Burn is a receiver-side
    semantic; the DHT ciphertext is not modified.

    ### Threat coverage

    - **Accidental same-machine re-decryption.** The user has multiple shells,
      scripts, or crontabs pointing at the same ciphertext; burn ensures only
      ONE of them succeeds. The ledger persists the burn-fact across reboots;
      a re-run of the same `cipherpost receive <uri>` invocation returns exit 7.
    - **UX awareness surface.** BURN-05 send-time stderr warning surfaces the
      local-state-only caveat BEFORE the user commits to send; the receive-time
      `[BURN — you will only see this once]` banner marker (D-P8-08) prepends
      the acceptance banner above the Purpose line. The user is informed twice.
    - **Receipt attestation preserved.** A receipt IS published on the first
      successful burn-receive (BURN-04). The sender sees a single `receipts
      --from <z32>` entry — burn does NOT suppress attestation.

    ### Threats NOT covered

    - **Multi-machine race.** Two receivers with fresh ledgers (different
      `CIPHERPOST_HOME` paths — e.g., a user with two laptops, or a script
      running in a fresh container) can BOTH successfully decrypt the same burn
      share until the share's TTL expires (24h default). Burn enforces only
      THIS machine's local consumption guard. There is no cross-machine
      coordination protocol.
    - **DHT-survives-TTL.** The encrypted payload remains in the Mainline DHT
      until TTL expires. Cipherpost cannot force-delete published ciphertext;
      the DHT is not under our control. A passive observer who captured the
      SignedPacket BEFORE the TTL expires can replay it forever.
    - **Cryptographic erasure.** Burn ≠ key destruction. The ciphertext can
      still be decrypted by any party with the receiver's identity passphrase
      (and the PIN, if pin_required) at any time before TTL expiry. Cipherpost
      makes no claim of cryptographic forward secrecy or post-burn key erasure.
    - **Tampered ledger.** A malicious receiver can manually edit
      `~/.cipherpost/state/accepted.jsonl` to remove a burned row; the next
      receive will succeed. This is OUT-OF-SCOPE: the burn property is between
      the user and their own ledger, not between users.

    ### Multi-machine race — explicit description

    ```
    Receiver A (~/.cipherpost-A/) — receives burn share, marks state=burned
    Receiver B (~/.cipherpost-B/) — fresh ledger, receives same share, succeeds
                                    — marks state=burned in ITS OWN ledger
    Both receivers see the plaintext. Both publish a receipt. Sender sees TWO
    receipts (one per receiver). The DHT ciphertext remains queryable until TTL.
    ```

    This is a **UX affordance, not a cryptographic destruction primitive**. Users
    needing strong single-consumption semantics across machines must look outside
    cipherpost (e.g., a centralized rate-limiting key server — explicit non-goal
    for v1.x per CLAUDE.md "no servers" principle).

    ### Atomicity invariant (D-P8-12)

    Burn flow uses **emit-before-mark** ordering (inverts v1.0 accepted-flow):
    `write_output` happens BEFORE `append_ledger_entry_with_state(Some("burned"))`.
    Crash between emit and ledger-write leaves the share re-receivable — safer
    failure mode than losing user data to a crashed state-write. v1.0 accepted-
    flow ordering is UNCHANGED.

    PITFALLS.md #26 (which originally recommended mark-then-emit for burn) is
    SUPERSEDED by D-P8-12. The original analysis is preserved with a
    SUPERSEDED header for historical context.

    ### Test references

    - `tests/burn_roundtrip.rs::burn_share_first_receive_succeeds_second_returns_exit_7` — BURN-09 + receipt-count==1 (BURN-04)
    - `tests/state_ledger.rs::v1_0_ledger_row_without_state_field_deserializes_as_accepted` — schema migration
    - `tests/state_ledger.rs::explicit_state_burned_deserializes_as_burned` — burn row recognition
    - `tests/pin_burn_compose.rs::typed_z32_declined_on_burn_share_does_not_mark_burned_and_share_remains_re_receivable` — declined-z32 safety
    - `tests/pin_burn_compose.rs::*_pin_burn_publishes_one_receipt` — receipt-count cross-cutting (4 typed-material variants)
    - `tests/pin_burn_compose.rs::*_burn_second_receive_exit_7` — second-receive cross-cutting (4 typed-material variants)

    ### Cross-references

    - SPEC.md §3.7 Burn Semantics — wire-shape, ledger inversion, receive-flow ordering
    - PITFALLS.md #25 — burn-is-local-state-only (original research; mitigation landed here)
    - PITFALLS.md #26 — SUPERSEDED by D-P8-12 (original mark-then-emit recommendation; preserved as rejected alternative)
    ```

    **Step D — Verify lychee link-check.**

    ```bash
    lychee THREAT-MODEL.md SPEC.md SECURITY.md README.md 2>&1 | tail -15
    ```

    Internal cross-references (SPEC.md §3.6 / §3.7 / PITFALLS.md #23..#26) must resolve. lychee may emit warnings on internal-anchor links if the section IDs don't auto-generate; if so, use plain markdown text references (e.g., "SPEC.md §3.6 PIN Crypto Stack") rather than anchor links.

    **DO NOT:**
    - DO NOT skip the indistinguishability invariant in PIN mode prose. PIN-10 explicitly requires this be documented; oracle hygiene is the foundation of PIN's threat model.
    - DO NOT skip the multi-machine race description in Burn mode prose. BURN-08 explicitly requires this caveat; users MUST see it in the threat model.
    - DO NOT cite tests that don't exist. Verify every test reference resolves to an actual file under tests/ before committing.
    - DO NOT renumber existing sections unless §6 doesn't currently use sub-numbering. Recommend §6.5 + §6.6 (additive); only escalate to §7 + §8 with renumber if the existing convention is flat.
    - DO NOT include implementation specifics (Argon2id parameter values, HKDF info string) — those live in SPEC.md. THREAT-MODEL.md is property + threat coverage, not protocol spec.
  </action>
  <verify>
    <automated>grep -q '6.5 PIN mode\|## §6.5\|## §7' THREAT-MODEL.md && grep -q '6.6 Burn mode\|## §6.6\|## §8' THREAT-MODEL.md && grep -q 'multi-machine race' THREAT-MODEL.md && grep -q 'wrong passphrase or identity decryption failed' THREAT-MODEL.md && grep -q 'emit-before-mark' THREAT-MODEL.md && grep -q 'burn ≠ cryptographic destruction\|burn ≠ key destruction\|burn != cryptographic destruction' THREAT-MODEL.md && lychee THREAT-MODEL.md 2>&1 | grep -qE 'OK|0 errors|warnings'</automated>
  </verify>
  <done>THREAT-MODEL.md ships §X.Y PIN mode + §X.Y Burn mode with full property + threat-coverage + threats-not-covered + multi-machine race description + emit-before-mark contract + test references + SPEC.md / PITFALLS.md cross-refs; lychee link-check clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: SPEC.md final consolidation pass + CLAUDE.md three new load-bearing lock-ins</name>
  <files>SPEC.md, CLAUDE.md</files>
  <read_first>
    - SPEC.md full (§3.6 PIN Crypto Stack from Plan 02; §3.7 Burn Semantics from Plan 04; §5.1 Send; §5.2 Receive; §6 Exit Codes; §Pitfall #22 wire-budget)
    - CLAUDE.md current "## Load-bearing lock-ins" bullet list (12 bullets currently per RESEARCH §"CLAUDE.md exact insertion targets")
    - .planning/phases/08-pin-and-burn-encryption-modes/08-RESEARCH.md §"CLAUDE.md exact insertion targets" (verbatim text + placement instructions)
    - .planning/phases/08-pin-and-burn-encryption-modes/08-CONTEXT.md §canonical_refs §"Spec sections to edit in Phase 8" §"CLAUDE.md §Load-bearing lock-ins additions"
  </read_first>
  <behavior>
    - SPEC.md §3.6 PIN Crypto Stack gains a paragraph or footnote linking to THREAT-MODEL.md §PIN mode (Plan 06 Task 1) for the threat-model treatment
    - SPEC.md §3.7 Burn Semantics gains a paragraph linking to THREAT-MODEL.md §Burn mode + PITFALLS.md #26 supersession
    - SPEC.md §5.1 Send: --pin and --burn flag documentation explicitly cross-references §3.6 / §3.7
    - SPEC.md §5.2 Receive: PIN prompt order + [BURN] marker placement explicitly cross-references §3.6 / §3.7
    - SPEC.md §6 Exit Codes: exit-4 row notes "wrong-PIN, wrong-passphrase, tampered-inner — see §3.6"; exit-7 row notes "share already consumed (burned) — see §3.7"
    - SPEC.md §Pitfall #22 (wire-budget): mentions pin+burn+typed-material composite size; cross-refs RESEARCH.md Open Risk #5 and Plan 05's wire-budget pre-flight test (`tests/pin_burn_compose.rs::pin_plus_burn_plus_pgp_wire_budget_surfaces_cleanly_or_succeeds`)
    - CLAUDE.md §Load-bearing lock-ins gains exactly 3 new bullets (or extensions to existing bullets) per Plan 06 final:
      * (a) HKDF info `cipherpost/v1/pin` added (sub-clause to existing HKDF bullet)
      * (b) accepted.jsonl row state field invariant + v1.0 default-deserialization (NEW bullet after ed25519-dalek pin)
      * (c) Burn write order = emit-before-mark per D-P8-12 (NEW bullet after Error-oracle hygiene)
    - lychee link-check passes
    - No source code changes
  </behavior>
  <action>
    **Step A — SPEC.md cross-reference updates.**

    Locate §3.6 PIN Crypto Stack (Plan 02 added). Append at the end of the section:

    ```markdown
    See THREAT-MODEL.md §PIN mode for the threat model — second-factor semantics,
    offline brute-force bound, intentional indistinguishability invariant, and
    multi-machine non-coordination caveat.
    ```

    Locate §3.7 Burn Semantics (Plan 04 added). Append at the end of the section:

    ```markdown
    See THREAT-MODEL.md §Burn mode for the threat model — multi-machine race,
    DHT-survives-TTL, burn ≠ cryptographic destruction. PITFALLS.md #26 carries
    the SUPERSEDED-by-D-P8-12 header preserving the rejected mark-then-emit
    alternative.
    ```

    Locate §5.1 Send (--pin and --burn flag documentation, added by Plans 02-03). Confirm both flag descriptions reference §3.6 / §3.7 — extend if missing.

    Locate §5.2 Receive (PIN prompt order + [BURN] marker, added by Plans 02-04). Confirm cross-refs to §3.6 / §3.7 — extend if missing.

    Locate §6 Exit Codes. The exit-4 row should mention "wrong-PIN, wrong-passphrase, tampered-inner — see §3.6 PIN Crypto Stack". The exit-7 row should mention "share already consumed (burned) — see §3.7 Burn Semantics".

    Locate §Pitfall #22 (wire-budget). Append:

    ```markdown
    Phase 8 wire-budget continuation: pin-required shares add ~165 B per nested-age
    layer + 32 B salt prefix; pin+burn+pgp-secret-key worst-case compose is
    predicted to brush the 1000 B BEP44 ceiling (RESEARCH.md Open Risk #5).
    `tests/pin_burn_compose.rs::pin_plus_burn_plus_pgp_wire_budget_surfaces_cleanly_or_succeeds`
    asserts the failure mode is a clean `Error::WireBudgetExceeded` (NOT a panic
    or Transport-internal error). Phase 9 DHT-07 measures empirically; v1.2
    ships the wire-budget escape hatch (chunking / two-tier storage / out-of-band).
    ```

    **Step B — CLAUDE.md load-bearing lock-ins.**

    Locate the section `## Load-bearing lock-ins (from research/PITFALLS.md, enforced in code + tests):` (per CLAUDE.md as displayed in env context). Find each insertion target.

    **Insertion 1 — extend the HKDF info bullet.** Current bullet (paraphrased):

    > HKDF info strings = **`cipherpost/v1/<context>`**. Never empty, never `None`. An enumeration test walks every HKDF call-site and asserts the prefix.

    Append a sub-clause:

    ```markdown
    Phase 8 adds `cipherpost/v1/pin` to the enumeration; the PIN-derived 32-byte
    X25519 scalar is wrapped into an `age::x25519::Identity` for nested
    encryption — `chacha20poly1305 only via age` invariant unchanged.
    ```

    **Insertion 2 — new bullet after the ed25519-dalek pin.** Insert:

    ```markdown
    - `~/.cipherpost/state/accepted.jsonl` rows carry an optional `state: "accepted"|"burned"` field; v1.0 rows missing the field deserialize via serde default to `accepted`. Burn rows write `state: "burned"` (D-P8-10). Schema migration is wire-format-additive — v1.0 receivers parse v1.1 rows by ignoring the new field. `check_already_consumed()` returns `LedgerState { None, Accepted, Burned }`; both call sites in `src/flow.rs::run_receive` and `src/main.rs` Send dispatch idempotency check pattern-match exhaustively.
    ```

    **Insertion 3 — new bullet after the Error-oracle hygiene bullet.** Insert:

    ```markdown
    - **Burn write order is emit-before-mark** (D-P8-12), inverting v1.0's accepted-then-emit ordering for burn shares ONLY. Crash between emit and ledger-write leaves share re-receivable — safer failure mode than losing user data to a crashed state-write. v1.0 accepted-flow is unchanged. `tests/burn_roundtrip.rs` + `tests/pin_burn_compose.rs` enforce; PITFALLS.md #26 carries SUPERSEDED-by-D-P8-12 header preserving the rejected mark-then-emit alternative.
    ```

    **Step B.5 — RESEARCH.md Open Questions audit hygiene (W6 fix).**

    Verify (and re-confirm if necessary) that `.planning/phases/08-pin-and-burn-encryption-modes/08-RESEARCH.md`'s `## Open Questions` heading reads `## Open Questions (RESOLVED)` AND each numbered question is prefixed with `RESOLVED — ` (e.g., `1. **RESOLVED — Should ...**`). The revision pass that produced this plan already applied these edits; Plan 06 confirms the audit trail is intact (does not gate execution; trivial doc-hygiene check).

    ```bash
    grep -q '^## Open Questions (RESOLVED)$' .planning/phases/08-pin-and-burn-encryption-modes/08-RESEARCH.md
    grep -cE '^[0-9]+\. \*\*RESOLVED — ' .planning/phases/08-pin-and-burn-encryption-modes/08-RESEARCH.md
    # Expect exit 0 + count >= 3 (three open questions all marked RESOLVED).
    ```

    If either check fails, apply the markers manually before completing Plan 06.

    **Step C — Verify.**

    ```bash
    lychee SPEC.md THREAT-MODEL.md SECURITY.md README.md 2>&1 | tail -10
    grep -q 'cipherpost/v1/pin' CLAUDE.md
    grep -q 'state.*accepted.*burned\|accepted\.jsonl.*state' CLAUDE.md
    grep -q 'emit-before-mark' CLAUDE.md
    grep -E '^- ' CLAUDE.md | wc -l   # confirm bullet count grew by ~2 (one extension + two new bullets)
    ```

    All checks must pass.

    **DO NOT:**
    - DO NOT modify the structure or numbering of existing CLAUDE.md sections. Append within the Load-bearing lock-ins bullet list only.
    - DO NOT remove any existing CLAUDE.md content. Plan 06 is purely additive.
    - DO NOT cite implementation file:line anchors in CLAUDE.md prose. The lock-ins are contract statements; they reference files (e.g., `src/flow.rs`) but not specific line numbers (which drift).
    - DO NOT introduce new SPEC.md sections beyond cross-reference updates. The §3.6 + §3.7 architecture sections landed in Plans 02 + 04; Plan 06 polishes cross-refs only.
  </action>
  <verify>
    <automated>grep -q 'cipherpost/v1/pin' CLAUDE.md && grep -q 'emit-before-mark' CLAUDE.md && grep -E 'state.*accepted.*burned|accepted\.jsonl.*state' CLAUDE.md && grep -q 'See THREAT-MODEL.md §PIN mode\|THREAT-MODEL.md.*PIN mode' SPEC.md && grep -q 'See THREAT-MODEL.md §Burn mode\|THREAT-MODEL.md.*Burn mode' SPEC.md && grep -q 'pin+burn+pgp\|pin\+burn\+pgp\|pin_plus_burn' SPEC.md && grep -q '^## Open Questions (RESOLVED)$' .planning/phases/08-pin-and-burn-encryption-modes/08-RESEARCH.md && lychee SPEC.md THREAT-MODEL.md README.md SECURITY.md 2>&1 | grep -qE '0 errors|OK'</automated>
  </verify>
  <done>SPEC.md cross-refs to THREAT-MODEL.md §PIN mode + §Burn mode added; SPEC.md §Pitfall #22 documents pin+burn+pgp wire-budget continuation; CLAUDE.md gains 3 new load-bearing lock-ins (HKDF info pin extension; ledger state field invariant; burn emit-before-mark contract); lychee link-check clean across all four docs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Future Claude instances → Phase 8 contracts | CLAUDE.md is the load-bearing entry point; future Claude instances see the three new lock-ins before touching code. Misreads bypass the contract → load-bearing failure. |
| External readers → security model | THREAT-MODEL.md is the readable threat statement; misframing the multi-machine race as a "race condition bug" rather than a documented out-of-scope property would lead users to misuse burn. |
| Spec readers → wire-format authority | SPEC.md is the protocol authority; cross-reference cleanup ensures readers don't implement v1.1+ behavior from §3.6 alone (which describes WHAT) without consulting THREAT-MODEL.md (which describes WHY). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-32 | Information Disclosure | Threat-model misframing of multi-machine race as a bug | mitigate | THREAT-MODEL.md §Burn mode explicitly describes the race as documented out-of-scope property; users see "burn ≠ cryptographic destruction" before adopting. |
| T-08-33 | Repudiation | Future Claude instances re-derive PIN/burn contracts from code | mitigate | CLAUDE.md load-bearing lock-ins document the three contracts (HKDF info, ledger state schema, emit-before-mark) — derived contracts no longer need re-derivation. |
| T-08-34 | Tampering | SPEC.md and THREAT-MODEL.md drift over time | accept | Plan 06 closes Phase 8; no per-phase drift mitigation. Future phases that touch PIN/burn semantics will need to update both docs. |
</threat_model>

<verification>
- THREAT-MODEL.md §X.Y PIN mode + §X.Y Burn mode landed (look for the "multi-machine race", "burn ≠ cryptographic destruction", "emit-before-mark", "wrong passphrase or identity decryption failed" literals)
- SPEC.md §3.6 / §3.7 cross-reference THREAT-MODEL.md
- SPEC.md §5.1, §5.2, §6, §Pitfall #22 polished
- CLAUDE.md §Load-bearing lock-ins extended with 3 new bullets (1 extension + 2 new)
- `lychee SPEC.md THREAT-MODEL.md SECURITY.md README.md` exit 0 (or warnings only on benign internal anchors)
- All 19 phase REQ-IDs covered: PIN-01..10 + BURN-01..09 (PIN-10 + BURN-08 land here in this plan; remaining 17 covered in Plans 01-05)
- No source code changes (cargo build still clean from Plan 05; no regression possible from docs-only edits)
</verification>

<success_criteria>
Phase 8 SHIPS COMPLETELY. All 19 REQ-IDs covered: PIN-01..10 + BURN-01..09. THREAT-MODEL.md documents PIN mode (second-factor semantics + indistinguishability + offline brute-force bound) and Burn mode (local-state-only + multi-machine race + DHT-survives-TTL + burn ≠ cryptographic destruction). SPEC.md is internally consistent with cross-references between §3.6 / §3.7 / §5.1 / §5.2 / §6 / §Pitfall #22. CLAUDE.md captures the three new load-bearing lock-ins for future Claude instances. Phase 9 (Real-DHT E2E + CAS race gate) is unblocked.
</success_criteria>

<output>
After completion, create `.planning/phases/08-pin-and-burn-encryption-modes/08-06-SUMMARY.md` documenting:

1. **PIN-10 + BURN-08 closure:** the two THREAT-MODEL.md REQ-IDs land here; full attacker-capabilities model + multi-machine race description + indistinguishability invariant.
2. **SPEC.md consolidation done:** cross-refs between §3.6 / §3.7 / §5.1 / §5.2 / §6 / §Pitfall #22 verified; lychee clean.
3. **CLAUDE.md load-bearing extensions:** three new bullets (HKDF pin info, ledger state schema, emit-before-mark contract); future Claude instances see the contracts.
4. **Phase 8 final coverage table:** every REQ-ID PIN-01..10 + BURN-01..09 with its primary plan number.
5. **RESEARCH.md Open Questions audit hygiene (W6):** confirms `## Open Questions (RESOLVED)` heading + per-question `RESOLVED — ` prefixes are present in `.planning/phases/08-pin-and-burn-encryption-modes/08-RESEARCH.md`. Edits applied during the revision pass (post-checker); Plan 06 verifies the audit trail.
6. **Pre-execution revision summary (B1/B2/B3 + W1-W6 resolutions):** A short audit-trail subsection enumerating the checker-feedback resolutions baked into Plans 01-06 BEFORE execution started — including the `transport.resolve` API correction (B2), the inline `count_receipts_for_share_ref` helper using `MockTransport::resolve_all_txt` (B1, applied to Plans 04 + 05), the PIN-08(c) concrete test landing exit-1 / Error::Config / no-state-mutation (B3), the PIN-02 generic-Display vs REQUIREMENTS wording resolution (W1, recorded in Plan 02 SUMMARY), the Plan-04 Task 1 cross-cutting Prompter trait checkpoint discipline (W2), the literal em-dash banner marker (W4), the path-helper visibility bump + `test_paths` cfg-gated re-export (W5, applied via Plan 03 Task 1 Step A0), and the RESEARCH.md Open Questions resolution markers (W6).
7. **Phase 9 hand-off:** STATE.md ready to advance; remaining v1.1 milestone work is Phase 9 (Real-DHT E2E + CAS merge-update race gate).
8. **Cross-milestone learnings (optional):** any patterns surfaced during Phase 8 worth recording for the next milestone retrospective (e.g., the Plan-04-pattern of "ship-gate plan = fixture + round-trip + oracle + leak-scan + SPEC update" applied across X509/PGP/SSH/PIN/BURN — five independent variants — without modification).
</output>
