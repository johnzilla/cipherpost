---
phase: 05-non-interactive-automation-e2e
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - SPEC.md
  - CLAUDE.md
  - tests/dht_label_constants.rs
  - .planning/milestones/v1.0-REQUIREMENTS.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [DOC-01, DOC-02, DOC-03, DOC-04]
must_haves:
  truths:
    - "SPEC.md §7 documents passphrase precedence as `fd > file > env > TTY` (matches shipped code per D-P5-01) and includes the exact six-case newline-strip truth table"
    - "SPEC.md §3.5 (new subsection inserted between §3.4 Receipt and §4) contains a wire-stability note for the DHT label constants (_cipherpost, _cprcpt-) — renames require a protocol_version bump"
    - "SPEC.md prose uses API-range form for crate versions (e.g., `serde_canonical_json (>= 1.0.0, RFC 8785 JCS); see Cargo.toml for exact pin`) — no hard-pinned numbers in prose"
    - "SPEC.md PKARR wire-budget prose says 550 bytes (matching measured v1.0 cut), not 600 bytes"
    - "DHT label constant-match unit test (tests/dht_label_constants.rs) asserts DHT_LABEL_OUTER == \"_cipherpost\" and DHT_LABEL_RECEIPT_PREFIX == \"_cprcpt-\" (renaming produces red CI); failure messages cite SPEC.md §3.5"
    - "CLAUDE.md has a `## Planning docs convention` section documenting the inline-phase-tag traceability rule"
    - ".planning/REQUIREMENTS.md PASS-05 line is rewritten to match the shipped precedence (fd > file > env > TTY, argv-inline rejected) per D-P5-01"
    - ".planning/milestones/v1.0-REQUIREMENTS.md's 49-row Complete traceability table has been deleted outright (format-convention removal — VERIFICATION.md files are now authoritative)"
  artifacts:
    - path: "SPEC.md"
      provides: "§7 precedence + strip-rule prose, new §3.5 DHT wire-stability subsection, §3/§4 version-prose rewrite, PKARR budget 550 B"
      contains: "fd > file > env > TTY"
    - path: "CLAUDE.md"
      provides: "New `## Planning docs convention` section with the inline-phase-tag rule and the forward-pointer to phase VERIFICATION.md files"
      contains: "Planning docs convention"
    - path: "tests/dht_label_constants.rs"
      provides: "Two unit tests byte-matching DHT_LABEL_OUTER and DHT_LABEL_RECEIPT_PREFIX against their SPEC.md §3.5 strings"
      exports: ["dht_label_outer_is_cipherpost_literal", "dht_label_receipt_prefix_is_cprcpt_literal"]
    - path: ".planning/REQUIREMENTS.md"
      provides: "PASS-05 line rewritten to match shipped precedence (fd > file > env > TTY); argv-inline rejection explicit"
      contains: "passphrase-fd > --passphrase-file > CIPHERPOST_PASSPHRASE > TTY"
    - path: ".planning/milestones/v1.0-REQUIREMENTS.md"
      provides: "49-row Complete traceability table removed; forward-pointer note installed at the deleted section's former location"
      contains: "Traceability format deprecated in v1.1"
  key_links:
    - from: "SPEC.md §3.5"
      to: "tests/dht_label_constants.rs"
      via: "the test asserts SPEC.md's documented labels == code constants; renaming either without renaming the other produces red CI"
      pattern: "DHT_LABEL_OUTER"
    - from: "SPEC.md §7 precedence rewrite"
      to: "src/identity.rs::resolve_passphrase"
      via: "SPEC.md now describes what the code ships (fd > file > env > TTY), not what it used to say; Plans 05-01/05-02 enforce the behavior"
      pattern: "fd > file > env > TTY"
    - from: ".planning/REQUIREMENTS.md PASS-05"
      to: "src/identity.rs::resolve_passphrase"
      via: "PASS-05 line now names the actual resolve_passphrase precedence; 05-02 code enforcement satisfies the requirement"
      pattern: "passphrase-fd > --passphrase-file > CIPHERPOST_PASSPHRASE > TTY"
    - from: "CLAUDE.md `Planning docs convention`"
      to: ".planning/REQUIREMENTS.md:7 Structure note"
      via: "CLAUDE.md references the convention; REQUIREMENTS.md defines it; future Claude sessions read CLAUDE.md first"
      pattern: "Planning docs convention"
---

<objective>
Close the v1.0 documentation debts in isolation from the code refactor — (1) rewrite SPEC.md §7 precedence + add the D-P5-08 strip-rule truth table, (2) add a new SPEC.md §3.5 "DHT Label Stability" subsection (inserted between §3.4 Receipt and §4) and back it with a unit test, (3) rewrite version-prose throughout SPEC.md to API-range form (Cargo.toml is exact-pin authority), (4) rewrite the PASS-05 line in `.planning/REQUIREMENTS.md` to match shipped precedence (BLOCKER 1), (5) document the inline-phase-tag traceability rule in CLAUDE.md, (6) delete the 49-row Complete traceability table from the archived v1.0 REQUIREMENTS.md (format-convention removal).

Purpose: This plan is pure documentation + one small constant-match test. It can run in parallel with Plan 05-01 (no code overlap) and has no dependency on Plans 05-01/05-02. It closes the four DOC requirements AND aligns PASS-05's wording with what 05-02 ships.

Output: SPEC.md edited in four sections (including new §3.5); CLAUDE.md gains a new top-level section; tests/dht_label_constants.rs created with 2 passing tests citing §3.5; .planning/REQUIREMENTS.md PASS-05 line rewritten; archived v1.0 REQUIREMENTS.md loses its 49-row Complete traceability table.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-non-interactive-automation-e2e/05-CONTEXT.md
@.planning/phases/05-non-interactive-automation-e2e/05-PATTERNS.md
@.planning/REQUIREMENTS.md
@.planning/research/PITFALLS.md
@SPEC.md
@CLAUDE.md
@Cargo.toml
@src/lib.rs

<interfaces>
<!-- DHT label constants under test. From src/lib.rs:33-38: -->
```rust
/// DHT label for outer share records (under the SENDER's PKARR key). D-05.
pub const DHT_LABEL_OUTER: &str = "_cipherpost";

/// DHT label prefix for receipts (under the RECIPIENT's PKARR key). D-06.
/// Full label: format!("{}{}", DHT_LABEL_RECEIPT_PREFIX, share_ref_hex).
pub const DHT_LABEL_RECEIPT_PREFIX: &str = "_cprcpt-";
```

<!-- SPEC.md current section layout (VERIFIED via awk on working tree): -->
<!--   Line 103: ### 3.1 Envelope -->
<!--   Line 132: ### 3.2 Material -->
<!--   Line 153: ### 3.3 OuterRecord -->
<!--   Line 186: ### 3.4 Receipt -->
<!--   Line 222: ## 4. Share URI -->
<!-- `_cipherpost` is currently mentioned inline in §3.3 OuterRecord (L155) and L263/L529. -->
<!-- `_cprcpt-` is currently mentioned inline in §3.4 Receipt (L188, L210-212) and L314-315, L326. -->
<!-- There is NO §3.3 "DHT Labels" subsection. Plan creates a new §3.5 subsection between §3.4 and §4. -->

<!-- CLAUDE.md location anchor: the new "Planning docs convention" heading goes AFTER the "## GSD workflow" section. -->

<!-- .planning/REQUIREMENTS.md PASS-05 target line (line 25): -->
<!--   `- [ ] **PASS-05 [Phase 5]**: Passphrase source precedence on send/receive matches `identity generate`/`show`: inline-rejected > env (`CIPHERPOST_PASSPHRASE`) > `--passphrase-file` > `--passphrase-fd` > TTY prompt` -->
<!-- This wording contradicts D-P5-01. Task 5 rewrites it inline. -->

<!-- Archive destructive-edit target: .planning/milestones/v1.0-REQUIREMENTS.md has a `## Traceability` section at line 195 -->
<!-- with 49 "Complete" rows (VERIFIED via `grep -cE "^\| [A-Z][A-Z0-9-]+-[0-9]+ +\| Phase [0-9]"` = 49). -->
<!-- There are 0 "Pending" rows today (VERIFIED). DOC-04 removes the 49-row table as a format-convention change, -->
<!-- not a stale-data cleanup. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite SPEC.md — §7 (passphrase precedence + strip rule), insert new §3.5 (DHT wire-stability), §3/§4 (API-range version prose + 550 B budget)</name>
  <files>SPEC.md</files>
  <read_first>
    - SPEC.md (the ENTIRE file — needed to locate §7 Passphrase Contract, §3.4 Receipt end, and §3/§4 version-mentioning paragraphs; the current prose shape must be preserved for everything except the targeted sentences)
    - .planning/phases/05-non-interactive-automation-e2e/05-CONTEXT.md §D-P5-01 (precedence `fd > file > env > TTY`), §D-P5-08 (six-case strip truth table), §D-P5-11 (version-prose rewrite rules + 550 B), §D-P5-12 (DHT wire-stability note)
    - .planning/phases/05-non-interactive-automation-e2e/05-PATTERNS.md §`SPEC.md` section (lines 627-637 — summary of all five SPEC.md edit points; note that PATTERNS.md erroneously references §3.3 for the wire-stability note; this task corrects that to §3.5, a NEW subsection)
    - .planning/research/PITFALLS.md #30 (strip rationale), #34 (version-prose pin hazard), #35 (env precedence security rationale)
    - Cargo.toml (exact-pin authority — verify current pins for serde_canonical_json, pkarr, ed25519-dalek, age; SPEC.md prose must NOT repeat these numbers)
    - tests/signed_packet_budget.rs (witness file for the 550 B budget number; SPEC.md prose should cite this test file as the measurement source)

    **Ground-truth section map (VERIFIED by orchestrator):** §3.1 Envelope (L103), §3.2 Material (L132), §3.3 OuterRecord (L153), §3.4 Receipt (L186), §4 Share URI (L222). `_cipherpost` is mentioned inline at L155 (§3.3), L263, L529. `_cprcpt-` is mentioned inline at L188 (§3.4), L210-212, L314-315, L326. There is NO existing §3.3 "DHT Labels" subsection. This task creates a new **§3.5 "DHT Label Stability"** subsection between §3.4 and §4.
  </read_first>
  <acceptance_criteria>
    - SPEC.md §7 (or §7.1 if subheader exists) first paragraph of precedence reads: "Passphrase sources are consulted in priority order: `fd > file > env (CIPHERPOST_PASSPHRASE) > TTY prompt`. Inline `--passphrase <value>` is rejected at parse/runtime."
    - SPEC.md §7 lists the four sources with one-line rationale each (per D-P5-01 and CONTEXT §specifics):
      - fd: no process-table exposure
      - file: no process-table exposure; inode permission gate (mode 0600 or 0400)
      - env: visible via `/proc/<pid>/environ` and `ps auxe` (Pitfall #35)
      - TTY: interactive only; cannot be scripted
    - SPEC.md §7 includes the six-case newline-strip truth table from D-P5-08 as a code-block truth table (all six rows):
      - `"hunter2\r\n"` → `"hunter2"` (CRLF stripped)
      - `"hunter2\n"` → `"hunter2"` (LF stripped)
      - `"hunter2\n\n"` → `"hunter2\n"` (one LF stripped; inner LF preserved)
      - `"hunter2 "` → `"hunter2 "` (trailing space preserved)
      - `"hunter2"` → `"hunter2"` (no trailer, untouched)
      - `"hunter2\r"` → `"hunter2\r"` (bare CR NOT stripped)
    - SPEC.md has a NEW `### 3.5 DHT Label Stability` subsection inserted between current §3.4 Receipt (L186) and current §4 Share URI (L222). The body documents `_cipherpost` and `_cprcpt-<share_ref_hex>` as wire-format constants and contains the wire-stability sentence.
    - **BLOCKER 2/4 — section-bounded grep:** `awk '/^### 3\.5 DHT Label Stability/,/^### 4\.|^## 4\./' SPEC.md | grep -q "Renaming either"` succeeds. And: `awk '/^### 3\.5 DHT Label Stability/,/^### 4\.|^## 4\./' SPEC.md | grep -q "protocol_version"` succeeds. Both greps must hit within §3.5 specifically, not anywhere else in SPEC.md.
    - SPEC.md §3 + §4 (anywhere the current prose hard-pins a crate version) rewritten to API-range form. Every mention of `serde_canonical_json 0.2` or `serde_canonical_json 1.0.0` or `pkarr 5.0.3` or `pkarr 5.0.4` or `ed25519-dalek =3.0.0-pre.5` in PROSE (not in code blocks that show actual Cargo.toml contents) is rewritten to the shape: `serde_canonical_json (>= 1.0.0, RFC 8785 JCS); see Cargo.toml for the exact pin in effect`. For `ed25519-dalek` specifically: SPEC prose mentions it only as "build constraint, not protocol guarantee" (D-P5-11 final paragraph).
    - SPEC.md PKARR wire-budget prose reads `550 bytes (measured at v1.0 cut; see tests/signed_packet_budget.rs)` — NOT `600 bytes`.
    - `! grep -q "600 byte" SPEC.md` (old budget number fully replaced).
    - `grep -q "fd > file > env > TTY" SPEC.md` succeeds.
    - `grep -c "_cipherpost" SPEC.md` returns at least 1 (inline in §3.3 per pre-existing prose AND documented in the new §3.5).
    - `grep -c "_cprcpt-" SPEC.md` returns at least 1 (inline in §3.4 per pre-existing prose AND documented in the new §3.5).
    - `! grep -q "serde_canonical_json 0.2" SPEC.md` (old version-prose fully replaced).
    - `! grep -q "serde_canonical_json 1.0.0" SPEC.md` (no hard-pin in prose; ranges only).
    - `lychee SPEC.md` (if the `lychee` CLI is installed) exits 0 — no broken cross-references after the rewrites.
  </acceptance_criteria>
  <action>
    Edit `SPEC.md` in place. Do NOT rename existing section headers; do NOT renumber §3.1, §3.2, §3.3, §3.4, §4, §7. All changes are (a) text-content rewrites within existing sections and (b) one NEW subsection insertion (§3.5).

    **Step A — §7 Passphrase Contract rewrite (D-P5-01 precedence + D-P5-08 strip rule).**

    Locate §7 in SPEC.md. Replace the current precedence ordering prose (which states `env > file > fd > TTY` — the OPPOSITE of what ships) with the new ordering and rationale table. Suggested prose structure (adapt to existing SPEC.md Markdown style):

    ```markdown
    ### 7.1 Precedence

    Passphrase sources are consulted in priority order:

    1. **`--passphrase-fd <N>`** — no process-table exposure; file-descriptor inherited from caller.
    2. **`--passphrase-file <PATH>`** — no process-table exposure; file must be mode `0600` or `0400` (inode permission gate).
    3. **`CIPHERPOST_PASSPHRASE` environment variable** — visible via `/proc/<pid>/environ` and `ps auxe` (Pitfall #35); use sparingly.
    4. **TTY prompt** — interactive only; cannot be scripted.

    Inline `--passphrase <value>` is **rejected** at parse/runtime (exit 4). Inline argv bytes leak via `/proc/<pid>/cmdline`, `ps`, and shell history.

    `--passphrase-fd 0` is rejected (stdin is reserved for payload I/O on `send`/`receive`).

    Setting both `--passphrase-file` and `--passphrase-fd` in a single invocation is rejected with exit 1.

    ### 7.2 Newline-strip rule

    Both `--passphrase-fd` and `--passphrase-file` strip **exactly one** trailing newline: one `\r\n`, else one `\n`, else nothing. Never a greedy `.trim()` (which would silently corrupt passphrases ending in space — Pitfall #30).

    Truth table:

    | Input bytes     | Stripped output |
    |-----------------|-----------------|
    | `hunter2\r\n`   | `hunter2`       |
    | `hunter2\n`     | `hunter2`       |
    | `hunter2\n\n`   | `hunter2\n`     |
    | `hunter2 `      | `hunter2 `      |
    | `hunter2`       | `hunter2`       |
    | `hunter2\r`     | `hunter2\r`     |
    ```

    **Step B — Insert NEW §3.5 "DHT Label Stability" subsection (BLOCKER 2 — corrects PATTERNS.md §3.3 mis-reference).**

    Locate the end of §3.4 Receipt (line 186 onward, runs to line 221 approximately — the last line before `## 4. Share URI` at line 222). Immediately BEFORE `## 4. Share URI`, insert a new `### 3.5 DHT Label Stability` subsection. Exact body:

    ```markdown
    ### 3.5 DHT Label Stability

    The DNS TXT record labels used in the wire format are part of the protocol surface:
    - `_cipherpost` — published by senders carrying `OuterRecord` (§3.3)
    - `_cprcpt-<share_ref_hex>` — published by recipients carrying `Receipt` (§3.4)

    These label strings are part of the wire format. Renaming either — in whole or in part
    — requires a `protocol_version` bump and a migration section in this SPEC. They are
    not changed silently.

    Code constants enforcing these labels are covered by a constant-match test
    (`tests/dht_label_constants.rs`) that fails if code and SPEC drift.
    ```

    Rationale for placement: `_cipherpost` is already mentioned inline in §3.3 OuterRecord; `_cprcpt-` is already mentioned inline in §3.4 Receipt. The wire-stability note covers BOTH, so its logical home is a dedicated subsection AFTER both, not inside either one. The new §3.5 sits at the end of Chapter 3 Wire Format, before Chapter 4 begins.

    **Step C — §3 and §4 version-prose rewrite (D-P5-11).**

    Walk every paragraph in §3 and §4 that mentions a crate by name with a version number. For each hit:
    - **serde_canonical_json** — rewrite prose to `serde_canonical_json (>= 1.0.0, RFC 8785 JCS); see Cargo.toml for the exact pin in effect`
    - **pkarr** — rewrite to `pkarr (>= 5.0.0); see Cargo.toml for the exact pin in effect`
    - **ed25519-dalek** — rewrite any prose mention to: `ed25519-dalek (matches pkarr's ed25519-dalek dependency — currently pinned exact in Cargo.toml as a build constraint, not a protocol guarantee; see CLAUDE.md §Load-bearing lock-ins)`
    - **age** — rewrite to `age (>= 0.10); see Cargo.toml for the exact pin in effect`

    **Do NOT** touch any fenced `toml` / `rust` code blocks that literally show Cargo.toml contents — those are accurate literals, not prose. Only rewrite running prose that states the version as fact.

    **Step D — PKARR wire-budget prose (D-P5-11 final paragraph).**

    Grep SPEC.md for `600 byte` / `600-byte` / `600 bytes` — every instance of the old PKARR wire budget number. Replace with `550 bytes (measured at v1.0 cut; see tests/signed_packet_budget.rs)`. The numeric constant stays cited (it's a protocol constraint, not a crate version).

    **Step E — final linkcheck.**

    If the repo has `lychee` installed and CI runs `lychee` over SPEC.md, re-verify that no cross-reference to §7, §3.5, §3, §4 broke. If an internal anchor name changed, either re-add the anchor OR update any SPEC.md paragraph that linked to it. Do NOT introduce a dangling link. Note: adding §3.5 is additive, so cross-references to §3.3 / §3.4 / §4 are unaffected.

    **Do NOT:**
    - Renumber §3.1, §3.2, §3.3, §3.4, §4, §7, or any other existing section.
    - Merge the new §3.5 into §3.3 or §3.4 — it covers both labels and deserves its own subsection (BLOCKER 2 decision).
    - Change `protocol_version` itself (still `1`; only a label/semantic change would bump it — and none is being made here).
    - Add version prose elsewhere in SPEC.md (§5, §6, §8+) unless a hard-pinned version is there too.
    - Touch `THREAT-MODEL.md` or `SECURITY.md` — those are out of this plan's scope.
  </action>
  <verify>
    <automated>grep -q "fd > file > env > TTY" SPEC.md &amp;&amp; awk '/^### 3\.5 DHT Label Stability/,/^### 4\.|^## 4\./' SPEC.md | grep -q "Renaming either" &amp;&amp; awk '/^### 3\.5 DHT Label Stability/,/^### 4\.|^## 4\./' SPEC.md | grep -q "protocol_version" &amp;&amp; grep -q "550 byte" SPEC.md &amp;&amp; ! grep -q "600 byte" SPEC.md &amp;&amp; ! grep -q "serde_canonical_json 1.0.0" SPEC.md &amp;&amp; ! grep -q "serde_canonical_json 0.2" SPEC.md &amp;&amp; grep -q "_cipherpost" SPEC.md &amp;&amp; grep -q "_cprcpt-" SPEC.md</automated>
  </verify>
  <done>
    - All grep invariants above pass in a single chained command (exit 0).
    - **BLOCKER 2 / 4 fix (section-bounded):** `awk '/^### 3\.5 DHT Label Stability/,/^### 4\.|^## 4\./' SPEC.md | grep -q "Renaming either"` and `... | grep -q "protocol_version"` both succeed — proves the wire-stability prose is inside §3.5, not diffused elsewhere.
    - `grep -n "hunter2" SPEC.md` returns at least 6 hits (the six rows of the strip truth table).
    - `grep -n "trim" SPEC.md` does NOT falsely suggest greedy trim is the rule — any remaining `trim` mention must be framed as anti-pattern.
    - If `lychee` is installed in the dev environment: `lychee SPEC.md` exits 0.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add tests/dht_label_constants.rs — two unit tests byte-matching DHT label constants against SPEC.md §3.5 literals</name>
  <files>tests/dht_label_constants.rs</files>
  <read_first>
    - src/lib.rs (confirm `DHT_LABEL_OUTER = "_cipherpost"` and `DHT_LABEL_RECEIPT_PREFIX = "_cprcpt-"` — lines 33-38 per 05-PATTERNS.md)
    - SPEC.md (Task 1 output — verify the new §3.5 subsection now documents both labels with the wire-stability note; the test's failure message cites §3.5, not §3.3)
    - .planning/phases/05-non-interactive-automation-e2e/05-CONTEXT.md §D-P5-12 (test-IS-the-audit decision; no standalone docs/dht-label-audit.md file)
    - .planning/phases/05-non-interactive-automation-e2e/05-PATTERNS.md §`tests/dht_label_constants.rs` section (lines 484-508 — contains the full test scaffold; PATTERNS.md cites §3.3 but this task OVERRIDES to §3.5 per BLOCKER 2)
    - tests/chacha20poly1305_direct_usage_ban.rs (analog — existing wire-constant-audit test pattern)
    - tests/mock_transport_roundtrip.rs (analog for the plain `#[test]` shape — no env mutation, no `#[serial]`)
  </read_first>
  <acceptance_criteria>
    - File `tests/dht_label_constants.rs` exists.
    - File contains exactly two `#[test]` functions: `dht_label_outer_is_cipherpost_literal` and `dht_label_receipt_prefix_is_cprcpt_literal`.
    - No `#[serial]` attribute (pure constant comparison; no env mutation, no fd, no tempdir).
    - Both tests import from the `cipherpost` crate (not a nested private module) — `use cipherpost::{DHT_LABEL_OUTER, DHT_LABEL_RECEIPT_PREFIX};`.
    - **BLOCKER 2 fix:** Each `assert_eq!` failure message mentions **SPEC.md §3.5** explicitly (NOT §3.3) so a developer who intentionally renames a constant knows what companion doc must be updated.
    - `cargo test --test dht_label_constants` exits 0 with 2 tests passing.
    - `grep -c "SPEC.md §3.5" tests/dht_label_constants.rs` returns 2 (one per test — BLOCKER 2 breadcrumb correction).
    - `! grep -q "SPEC.md §3.3" tests/dht_label_constants.rs` (no stale §3.3 reference).
  </acceptance_criteria>
  <behavior>
    - Test `dht_label_outer_is_cipherpost_literal` asserts `DHT_LABEL_OUTER == "_cipherpost"` byte-for-byte. Failure message: "SPEC.md §3.5 locks this label; renaming requires a protocol_version bump".
    - Test `dht_label_receipt_prefix_is_cprcpt_literal` asserts `DHT_LABEL_RECEIPT_PREFIX == "_cprcpt-"` byte-for-byte. Same failure-message template, citing §3.5.
  </behavior>
  <action>
    Create `tests/dht_label_constants.rs`. **BLOCKER 2 correction:** failure messages cite §3.5 (the new subsection from Task 1), not §3.3. Full file content:

    ```rust
    //! DOC-02 / Pitfall #33: DHT label strings are wire-protocol constants.
    //! Renaming either requires a protocol_version bump. This test is the
    //! "confirm, don't change" audit — it byte-matches code constants against
    //! the values documented in SPEC.md §3.5 DHT Label Stability.

    use cipherpost::{DHT_LABEL_OUTER, DHT_LABEL_RECEIPT_PREFIX};

    #[test]
    fn dht_label_outer_is_cipherpost_literal() {
        assert_eq!(
            DHT_LABEL_OUTER, "_cipherpost",
            "SPEC.md §3.5 locks this label; renaming requires a protocol_version bump"
        );
    }

    #[test]
    fn dht_label_receipt_prefix_is_cprcpt_literal() {
        assert_eq!(
            DHT_LABEL_RECEIPT_PREFIX, "_cprcpt-",
            "SPEC.md §3.5 locks this label; renaming requires a protocol_version bump"
        );
    }
    ```

    **Do NOT** inline this test into `src/transport.rs` — keep it as an integration-test file per 05-PATTERNS.md recommendation (cleaner grep, same strictness, matches the precedent of `tests/chacha20poly1305_direct_usage_ban.rs`).

    **Do NOT** add a third test checking the overall format `_cprcpt-<hex>` — the prefix itself is the contract; the full-label format is exercised by transport-layer integration tests.

    **Do NOT** reference the constants via a hypothetical `cipherpost::transport::*` path — the constants live at `cipherpost::DHT_LABEL_OUTER` and `cipherpost::DHT_LABEL_RECEIPT_PREFIX` (re-exported from src/lib.rs per the pattern shown in 05-PATTERNS.md lines 349-355).

    **Do NOT** cite §3.3 in failure messages (BLOCKER 2 — PATTERNS.md scaffold is wrong about section number; use §3.5).
  </action>
  <verify>
    <automated>cargo test --test dht_label_constants 2>&amp;1 | tail -10</automated>
  </verify>
  <done>
    - `cargo test --test dht_label_constants` prints `test result: ok. 2 passed; 0 failed`.
    - `grep -c "^#\[test\]$" tests/dht_label_constants.rs` returns 2.
    - `grep -c "#\[serial\]" tests/dht_label_constants.rs` returns 0.
    - **BLOCKER 2 fix:** `grep -c "SPEC.md §3.5" tests/dht_label_constants.rs` returns 2 (failure-message breadcrumb in both assertions, citing the correct section).
    - **BLOCKER 2 fix:** `! grep -q "SPEC.md §3.3" tests/dht_label_constants.rs` (no stale §3.3 reference).
    - `cargo test` (full suite) still exits 0 — the new test does not interfere with existing tests.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add `## Planning docs convention` section to CLAUDE.md</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (the ENTIRE file — needed to locate the `## GSD workflow` section; the new `## Planning docs convention` section is inserted immediately after it, NOT at the end of file; 05-CONTEXT.md §D-P5-14 explains the adjacency rationale — "sits near related meta-instructions")
    - .planning/phases/05-non-interactive-automation-e2e/05-CONTEXT.md §D-P5-14 (reference wording — but this task OVERRIDES the "29 stale" phrasing per BLOCKER 3; the archived table today has 0 Pending rows and 49 Complete rows) and §D-P5-03 (convention-change-documented-in-both-places rationale)
    - .planning/phases/05-non-interactive-automation-e2e/05-PATTERNS.md §`CLAUDE.md` section (lines 641-644 — placement instruction)
    - .planning/REQUIREMENTS.md:7 (the Structure note — CLAUDE.md section references this source of truth)
  </read_first>
  <acceptance_criteria>
    - CLAUDE.md gains a new top-level heading `## Planning docs convention` immediately after the `## GSD workflow` section (not at end of file).
    - **BLOCKER 3 fix:** The section body uses the REWRITTEN wording (below) — it does NOT mention "29 stale Pending rows" because that narrative is counterfactual (the archived table had 0 Pending rows by the time it was archived; DOC-04 removes 49 Complete rows as a format-convention change, not a stale-data cleanup).
    - The section body contains the rewritten paragraph verbatim (see action block).
    - `grep -q "## Planning docs convention" CLAUDE.md` succeeds.
    - `grep -q "per-phase VERIFICATION.md" CLAUDE.md` succeeds (anchor phrase from the rewritten paragraph).
    - `! grep -q "29 stale" CLAUDE.md` (BLOCKER 3 — old counterfactual phrasing is NOT reintroduced anywhere in CLAUDE.md).
    - `grep -n "## Planning docs convention" CLAUDE.md` appears at a line number GREATER than the line number of `## GSD workflow` — adjacency verified.
    - No other CLAUDE.md section is modified (existing `## Load-bearing lock-ins`, `## GSD workflow`, etc. are untouched).
  </acceptance_criteria>
  <action>
    **BLOCKER 3 — REWRITTEN wording (overrides the D-P5-14 text that cites "29 stale Pending rows").** The rewrite states what the convention actually prevents (parallel-table drift class) without tying it to a specific row-count claim that no longer matches the archive.

    1. Read CLAUDE.md. Locate the `## GSD workflow` heading (expected location per 05-PATTERNS.md line 643 — "after the existing ## GSD workflow section").
    2. Find the END of the `## GSD workflow` section (the line immediately before the next top-level `## ` heading, or EOF if GSD workflow is the last section).
    3. Insert a blank line, then this exact block:

    ```markdown
    ## Planning docs convention

    **Requirements traceability.** Each requirement in `.planning/REQUIREMENTS.md` carries an
    inline `[Phase N]` tag and a checkbox. Phase `.planning/phases/<NN>/VERIFICATION.md`
    files are authoritative for implementation status. Do not add or regenerate a separate
    traceability table — maintaining a parallel table risks a drift class where table rows
    fall behind the body checkboxes and the per-phase verification reports.
    ```

    4. If CLAUDE.md has additional sections after `## GSD workflow`, place the new section BEFORE those subsequent sections (adjacent to GSD workflow, per D-P5-14 "sits near related meta-instructions"). If `## GSD workflow` is the final section in CLAUDE.md, append the new section at EOF with a leading blank line.

    **Do NOT:**
    - Modify the `## Load-bearing lock-ins` section or any other existing section.
    - Use the CONTEXT.md §D-P5-14 paragraph verbatim — BLOCKER 3 replaces the "29 stale Pending rows" sentence with the drift-class wording shown above.
    - Add sub-headings inside `## Planning docs convention` — one paragraph only.
    - Reference CLAUDE.md from itself (`see CLAUDE.md`) — the paragraph references `.planning/REQUIREMENTS.md` and `.planning/phases/<NN>/VERIFICATION.md` as sources of truth.
  </action>
  <verify>
    <automated>grep -q "## Planning docs convention" CLAUDE.md &amp;&amp; grep -q "per-phase verification reports" CLAUDE.md &amp;&amp; ! grep -q "29 stale" CLAUDE.md &amp;&amp; [ $(grep -n "## Planning docs convention" CLAUDE.md | cut -d: -f1) -gt $(grep -n "## GSD workflow" CLAUDE.md | cut -d: -f1) ]</automated>
  </verify>
  <done>
    - `grep -q "## Planning docs convention" CLAUDE.md` succeeds.
    - `grep -q "inline .\\[Phase N\\]. tag" CLAUDE.md` succeeds (anchor phrase).
    - `grep -q "per-phase verification reports" CLAUDE.md` succeeds (anchor phrase from rewrite).
    - **BLOCKER 3 fix:** `! grep -q "29 stale" CLAUDE.md` (counterfactual phrase NOT present).
    - Adjacency verified: `## Planning docs convention` line number > `## GSD workflow` line number.
    - `git diff CLAUDE.md` shows only an addition (no deletion of existing lines, no reflow of unrelated prose).
    - `grep -c "^## " CLAUDE.md` increased by exactly 1 versus the pre-change count.
  </done>
</task>

<task type="auto">
  <name>Task 4: Delete 49-row Complete traceability table from .planning/milestones/v1.0-REQUIREMENTS.md and install forward-pointer note</name>
  <files>.planning/milestones/v1.0-REQUIREMENTS.md</files>
  <read_first>
    - .planning/milestones/v1.0-REQUIREMENTS.md (the ENTIRE file — needed to locate the `## Traceability` section at line 195 and understand what surrounding sections to preserve; VERIFIED ground truth: 49 Complete rows, 0 Pending rows today)
    - .planning/phases/05-non-interactive-automation-e2e/05-CONTEXT.md §D-P5-13 (exact forward-pointer text; commit message `docs(archive): drop v1.0 traceability table (DOC-04)` is locked)
    - .planning/phases/05-non-interactive-automation-e2e/05-PATTERNS.md §`.planning/milestones/v1.0-REQUIREMENTS.md` section (lines 647-649 — destructive-edit-only confirmation)
    - .planning/REQUIREMENTS.md:7 (Structure note — the living convention the archived file is deferring to)
  </read_first>
  <acceptance_criteria>
    - **BLOCKER 3/5 — positive assertions (not Pending==0):** The 49-row Complete traceability table is gone. Specifically:
      - `! grep -qE "^## Traceability$|^## Requirements Traceability$" .planning/milestones/v1.0-REQUIREMENTS.md` (no traceability section header survives)
      - `[ $(grep -cE "^\| [A-Z][A-Z0-9-]+-[0-9]+ +\| Phase [0-9]" .planning/milestones/v1.0-REQUIREMENTS.md) -eq 0 ]` (no SCAF-01/CRYPTO-01-style rows survive)
      - `[ $(grep -cE "^\| Complete \|" .planning/milestones/v1.0-REQUIREMENTS.md) -eq 0 ]` (all 49 "Complete" cells removed)
      - `grep -q "Traceability format deprecated in v1.1" .planning/milestones/v1.0-REQUIREMENTS.md` (forward-pointer added)
    - At the deleted section's former location (or an equivalent location if the surrounding structure prefers), the following blockquote paragraph (verbatim from D-P5-13) has been inserted:
      ```
      > Traceability format deprecated in v1.1.
      > Per-requirement implementation status: see
      > .planning/milestones/v1.0-phases/<NN>/VERIFICATION.md per phase.
      > (Convention change documented in CLAUDE.md "Planning docs convention.")
      ```
    - Sections UNRELATED to traceability are preserved unchanged (v1 Requirements, v2 Requirements, Out of Scope — do not touch those).
    - `grep -c "^## " .planning/milestones/v1.0-REQUIREMENTS.md` decreased by exactly 1 (Traceability section header removed; no other section added or removed).
  </acceptance_criteria>
  <action>
    **BLOCKER 3 reframing:** DOC-04's real work is dropping the 49-Complete-row traceability table for format-convention reasons (convention now lives in inline checkboxes + per-phase VERIFICATION.md). It is NOT a stale-data cleanup — the table has 0 "Pending" rows today (verified by orchestrator). The Pending==0 grep of the previous draft was trivially satisfied before any change. The tightened acceptance uses POSITIVE assertions on the 49 Complete rows.

    1. Read the entire `.planning/milestones/v1.0-REQUIREMENTS.md` file. Locate the `## Traceability` section (at line 195 per ground truth; section contains 49 markdown-table rows with a `Complete` status cell and a `Phase N` cell per requirement).
    2. Delete the ENTIRE `## Traceability` section — the heading, any introductory prose for the section, the table header (`| ID | Phase | Status |` or similar), the alignment row (`|---|---|---|`), and every one of the 49 data rows. This is a surgical delete: preserve everything above the section's heading (the v1 Requirements, v2 Requirements, Out of Scope sections at lines 16/139/176) unchanged.
    3. At the deleted section's former location (where the `## Traceability` heading used to be — around line 195), insert this blockquote verbatim (no modifications):

    ```markdown
    > Traceability format deprecated in v1.1.
    > Per-requirement implementation status: see
    > .planning/milestones/v1.0-phases/<NN>/VERIFICATION.md per phase.
    > (Convention change documented in CLAUDE.md "Planning docs convention.")
    ```

    4. Do NOT retain the `## Traceability` heading. The section is gone; the blockquote sits where it used to be. If a table of contents exists at the top of the file referencing the section, remove that TOC entry too; otherwise the TOC will be a dangling link.

    **Do NOT:**
    - Touch any other section — `## v1 Requirements`, `## v2 Requirements`, `## Out of Scope` all stay untouched.
    - Regenerate the table in a "cleaner" form — the convention is to drop the format entirely.
    - Add new prose explaining the drop beyond the four-line blockquote.
    - Modify the living `.planning/REQUIREMENTS.md` — Task 5 handles that file separately.

    **Commit-message reminder** (for the dev running execute-phase): the commit that lands this change should use the message `docs(archive): drop v1.0 traceability table (DOC-04)` per D-P5-13.
  </action>
  <verify>
    <automated>grep -q "Traceability format deprecated in v1.1" .planning/milestones/v1.0-REQUIREMENTS.md &amp;&amp; ! grep -qE "^## Traceability$|^## Requirements Traceability$" .planning/milestones/v1.0-REQUIREMENTS.md &amp;&amp; [ $(grep -cE "^\| [A-Z][A-Z0-9-]+-[0-9]+ +\| Phase [0-9]" .planning/milestones/v1.0-REQUIREMENTS.md) -eq 0 ] &amp;&amp; [ $(grep -cE "^\| Complete \|" .planning/milestones/v1.0-REQUIREMENTS.md) -eq 0 ] &amp;&amp; grep -q "Convention change documented in CLAUDE.md" .planning/milestones/v1.0-REQUIREMENTS.md</automated>
  </verify>
  <done>
    - Blockquote present: `grep -q "Traceability format deprecated in v1.1" .planning/milestones/v1.0-REQUIREMENTS.md` succeeds.
    - Forward-pointer present: `grep -q "Convention change documented in CLAUDE.md" .planning/milestones/v1.0-REQUIREMENTS.md` succeeds.
    - **BLOCKER 3/5 positive assertions:**
      - `! grep -qE "^## Traceability$|^## Requirements Traceability$" .planning/milestones/v1.0-REQUIREMENTS.md` (header gone)
      - `grep -cE "^\| [A-Z][A-Z0-9-]+-[0-9]+ +\| Phase [0-9]" .planning/milestones/v1.0-REQUIREMENTS.md` returns 0 (no SCAF-/CRYPTO- rows)
      - `grep -cE "^\| Complete \|" .planning/milestones/v1.0-REQUIREMENTS.md` returns 0 (all 49 Complete cells gone)
    - The file still has its original requirement definitions (`## v1 Requirements`, `## v2 Requirements`, `## Out of Scope`) intact — spot-check that their line counts are unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 5: Rewrite PASS-05 line in .planning/REQUIREMENTS.md to match shipped precedence (BLOCKER 1)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (line 25 specifically — current PASS-05 wording contradicts D-P5-01 / shipped resolve_passphrase). The entire PASS section for context.
    - .planning/phases/05-non-interactive-automation-e2e/05-CONTEXT.md §D-P5-01 (locked precedence: `fd > file > env > TTY`, argv-inline rejected) — this task aligns PASS-05 text with that decision.
    - Plan 05-02's code enforcement — the PASS-05 requirement ID stays on Plan 05-02 (code), this task rewrites the REQUIREMENT TEXT only.
  </read_first>
  <acceptance_criteria>
    - Line 25 of `.planning/REQUIREMENTS.md` is rewritten from the current (contradictory) text to align with D-P5-01.
    - **BLOCKER 1 — exact wording:**
      ```
      - [ ] **PASS-05 [Phase 5]**: Passphrase source precedence on `send`/`receive` matches the shipped `resolve_passphrase` contract: `--passphrase-fd > --passphrase-file > CIPHERPOST_PASSPHRASE > TTY`. Argv-inline `--passphrase <value>` is rejected at parse and at runtime. Identity subcommands (`generate`/`show`) use the same ordering.
      ```
    - `grep -q "passphrase-fd > --passphrase-file > CIPHERPOST_PASSPHRASE > TTY" .planning/REQUIREMENTS.md` succeeds.
    - `! grep -q "env (CIPHERPOST_PASSPHRASE) > --passphrase-file > --passphrase-fd" .planning/REQUIREMENTS.md` (old contradictory text is gone).
    - PASS-05 checkbox state is preserved (still `[ ]` — unchecked — because the requirement is satisfied in Phase 5 but this doc edit doesn't mark it done; Plan 05-02's execution will close the checkbox as part of the code work).
    - No OTHER requirement line in `.planning/REQUIREMENTS.md` is modified. Only line 25.
  </acceptance_criteria>
  <action>
    1. Read `.planning/REQUIREMENTS.md` around line 25.
    2. The current line reads (per ground truth):
       ```
       - [ ] **PASS-05 [Phase 5]**: Passphrase source precedence on `send`/`receive` matches `identity generate`/`show`: inline-rejected > env (`CIPHERPOST_PASSPHRASE`) > `--passphrase-file` > `--passphrase-fd` > TTY prompt
       ```
       This wording states the OPPOSITE of what `resolve_passphrase` ships (fd > file > env > TTY). Plan 05-01 / 05-02 will not change the shipped precedence — D-P5-01 locked it. This task fixes the requirement text.
    3. Replace line 25 with:
       ```
       - [ ] **PASS-05 [Phase 5]**: Passphrase source precedence on `send`/`receive` matches the shipped `resolve_passphrase` contract: `--passphrase-fd > --passphrase-file > CIPHERPOST_PASSPHRASE > TTY`. Argv-inline `--passphrase <value>` is rejected at parse and at runtime. Identity subcommands (`generate`/`show`) use the same ordering.
       ```
    4. Leave the checkbox as `[ ]` (unchecked). Plan 05-02's execution will close it when PASS-05's code enforcement ships.
    5. Do NOT touch any other line in `.planning/REQUIREMENTS.md`.

    **Note on requirement attribution:** PASS-05 is already listed in Plan 05-02's frontmatter `requirements` array (that's where the code enforcement lives). This task does NOT move PASS-05 between plans — it only aligns the requirement TEXT with the code. The 05-02 task inventory is unchanged.

    **Why this task lives in 05-03:** This plan is the doc-housekeeping plan; PASS-05's text is a planning-document fix, not a code change. Adding it to 05-03 preserves the wave-1-parallel structure (05-01 + 05-03 run in parallel; 05-02 depends on 05-01).
  </action>
  <verify>
    <automated>grep -q "passphrase-fd > --passphrase-file > CIPHERPOST_PASSPHRASE > TTY" .planning/REQUIREMENTS.md &amp;&amp; ! grep -q "env (CIPHERPOST_PASSPHRASE) > --passphrase-file > --passphrase-fd" .planning/REQUIREMENTS.md &amp;&amp; grep -q "^- \[ \] \*\*PASS-05" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>
    - **BLOCKER 1 fix:** `grep -q "passphrase-fd > --passphrase-file > CIPHERPOST_PASSPHRASE > TTY" .planning/REQUIREMENTS.md` succeeds.
    - **BLOCKER 1 fix:** `! grep -q "env (CIPHERPOST_PASSPHRASE) > --passphrase-file > --passphrase-fd" .planning/REQUIREMENTS.md` (old contradictory text is gone).
    - Checkbox state preserved: `grep -q "^- \[ \] \*\*PASS-05" .planning/REQUIREMENTS.md` succeeds.
    - `git diff .planning/REQUIREMENTS.md` shows a single-line replacement at line 25; no other lines touched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SPEC.md prose → developer understanding | SPEC.md is the source of truth for wire format; prose errors leak into future implementations that treat prose as fact (e.g., a contributor who reads "env > file > fd" would implement the wrong precedence and silently change unlock behavior). |
| DHT label constants (code) ↔ SPEC.md §3.5 label documentation | The wire format depends on byte identity between documented label string and code constant. Drift would create an interoperability break that is hard to diagnose (receiver looks at wrong DNS label). |
| Archived planning docs → present-day inference | Stale requirements-vs-table-status rows in v1.0-REQUIREMENTS.md mislead a future auditor. Even with 0 Pending rows today, the parallel-table format itself is the drift risk. |
| `.planning/REQUIREMENTS.md` PASS-05 text ↔ shipped code | A requirement line that contradicts shipped behavior (current PASS-05 says env-first, code ships fd-first) is a latent bug source: a future contributor reading the requirement could refactor code to "fix" the disagreement by downgrading security. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-14 | Tampering | SPEC.md precedence prose disagrees with shipped code (`env > file > fd` in SPEC, `fd > file > env` in code) — a future contributor trusting SPEC as source-of-truth could refactor code to match SPEC, silently downgrading security (env is priority 4, not priority 1) | mitigate | Task 1 rewrites SPEC.md §7 to match shipped code (D-P5-01). Rewriting code to match stale SPEC was rejected because it would silently change unlock behavior for existing users. |
| T-05-15 | Tampering | DHT label constants (`_cipherpost`, `_cprcpt-`) changed in code without SPEC.md §3.5 update — receiver looks at wrong DNS label, interop break | mitigate | D-P5-12: Task 2's byte-match unit test fails CI if either constant is edited without the companion SPEC.md update (the test's failure message cites SPEC.md §3.5 so developers know what to update). |
| T-05-16 | Repudiation | Archived v1.0-REQUIREMENTS.md traceability table — even with 0 Pending rows today, the parallel-table format is the drift class. A future milestone could re-introduce Pending rows that fall out of sync with per-phase VERIFICATION.md files. | mitigate | D-P5-13 / Task 4: delete the 49-Complete-row table outright; forward-pointer explicitly names phase VERIFICATION.md files as authoritative. No parallel table = no drift class. |
| T-05-17 | Information Disclosure | SPEC.md version-prose includes hard-pinned version numbers that drift from Cargo.toml — a protocol-auditing entity (security researcher, CVE reviewer) comparing SPEC to Cargo.toml sees disagreement and cannot tell which is authoritative | mitigate | D-P5-11 / Task 1: SPEC prose uses API ranges (`>= 1.0.0`); Cargo.toml is the stated single source of truth. No version drift possible because SPEC prose no longer contains specific numbers. |
| T-05-18 | Elevation of Privilege (informational) | A malicious PR renames a DHT label constant silently in code, hoping the companion SPEC update slips through review | accept | The constant-match test forces a red CI. Reviewer sees both the code constant and the SPEC change side-by-side — reviewer can reject a rename that lacks a `protocol_version` bump. This is process + tooling, not technical enforcement. |
| T-05-19 | Information Disclosure | CLAUDE.md convention paragraph drift — if the convention paragraph in CLAUDE.md disagrees with `.planning/REQUIREMENTS.md:7`, future sessions get conflicting guidance | mitigate | D-P5-14: both places carry the rule; CLAUDE.md references REQUIREMENTS.md:7 as "where the rule is defined." The convention paragraph is stable and short — low drift surface. |
| T-05-20 | Tampering | `.planning/REQUIREMENTS.md` PASS-05 line contradicts shipped precedence — future contributor reads the requirement, "fixes" the code to match, silently downgrades security (env as priority 1) | mitigate | Task 5 rewrites PASS-05 inline to match D-P5-01 / resolve_passphrase. The new wording explicitly names fd > file > env > TTY. Plan 05-02's code enforcement closes the loop. |

**High-severity threats blocking plan completion:** none. T-05-14, T-05-15, T-05-16, T-05-17, T-05-20 are all mitigated within Tasks 1–5 of this plan.
</threat_model>

<verification>
**Automated gates:**

- `cargo test` (full suite) exits 0 — the new `tests/dht_label_constants.rs` integrates cleanly.
- `cargo test --test dht_label_constants` exits 0 with 2 tests passing.
- `cargo fmt --check` exits 0.
- `cargo clippy -- -D warnings` exits 0 (no impact — no source code changed in this plan).

**SPEC.md invariants (Task 1):**

- `grep -q "fd > file > env > TTY" SPEC.md` succeeds.
- **BLOCKER 2/4 section-bounded:** `awk '/^### 3\.5 DHT Label Stability/,/^### 4\.|^## 4\./' SPEC.md | grep -q "Renaming either"` succeeds.
- **BLOCKER 2/4 section-bounded:** `awk '/^### 3\.5 DHT Label Stability/,/^### 4\.|^## 4\./' SPEC.md | grep -q "protocol_version"` succeeds.
- `grep -q "550 byte" SPEC.md` succeeds.
- `! grep -q "600 byte" SPEC.md` (old budget gone).
- `! grep -q "serde_canonical_json 1.0.0" SPEC.md` and `! grep -q "serde_canonical_json 0.2" SPEC.md` (no hard-pinned versions in prose).
- `grep -q "_cipherpost" SPEC.md` and `grep -q "_cprcpt-" SPEC.md` succeed.
- If `lychee` is available: `lychee SPEC.md` exits 0 — no broken cross-references.

**CLAUDE.md invariants (Task 3):**

- `grep -q "## Planning docs convention" CLAUDE.md` succeeds.
- **BLOCKER 3:** `! grep -q "29 stale" CLAUDE.md` (counterfactual phrase NOT present anywhere in CLAUDE.md).
- `grep -q "per-phase verification reports" CLAUDE.md` succeeds.
- `## Planning docs convention` appears AFTER `## GSD workflow` (adjacency).

**v1.0-REQUIREMENTS.md invariants (Task 4):**

- `grep -q "Traceability format deprecated in v1.1" .planning/milestones/v1.0-REQUIREMENTS.md` succeeds.
- **BLOCKER 3/5 positive assertions:**
  - `! grep -qE "^## Traceability$|^## Requirements Traceability$" .planning/milestones/v1.0-REQUIREMENTS.md`
  - `[ $(grep -cE "^\| [A-Z][A-Z0-9-]+-[0-9]+ +\| Phase [0-9]" .planning/milestones/v1.0-REQUIREMENTS.md) -eq 0 ]`
  - `[ $(grep -cE "^\| Complete \|" .planning/milestones/v1.0-REQUIREMENTS.md) -eq 0 ]`

**.planning/REQUIREMENTS.md invariants (Task 5 — BLOCKER 1):**

- `grep -q "passphrase-fd > --passphrase-file > CIPHERPOST_PASSPHRASE > TTY" .planning/REQUIREMENTS.md` succeeds.
- `! grep -q "env (CIPHERPOST_PASSPHRASE) > --passphrase-file > --passphrase-fd" .planning/REQUIREMENTS.md`.

**Unit-test invariants (Task 2 — BLOCKER 2):**

- `grep -q "DHT_LABEL_OUTER" tests/dht_label_constants.rs` succeeds.
- `grep -q "DHT_LABEL_RECEIPT_PREFIX" tests/dht_label_constants.rs` succeeds.
- `grep -c "SPEC.md §3.5" tests/dht_label_constants.rs` returns 2.
- `! grep -q "SPEC.md §3.3" tests/dht_label_constants.rs` (stale §3.3 reference not reintroduced).
- No `#[serial]` attribute present.
</verification>

<success_criteria>
Plan 05-03 is complete when:

1. SPEC.md §7 documents `fd > file > env > TTY` precedence and the D-P5-08 six-case strip truth table; a NEW §3.5 "DHT Label Stability" subsection (between §3.4 and §4) documents `_cipherpost` and `_cprcpt-` with the wire-stability note; §3/§4 prose is version-range-only (no hard-pinned crate numbers); PKARR wire budget is documented as 550 B.
2. `tests/dht_label_constants.rs` has 2 passing unit tests that fail red if either DHT label constant drifts from its SPEC.md literal. Failure messages cite §3.5 (BLOCKER 2).
3. CLAUDE.md has a new `## Planning docs convention` section adjacent to `## GSD workflow` with the BLOCKER-3 rewritten paragraph (no "29 stale" language).
4. `.planning/milestones/v1.0-REQUIREMENTS.md` has the 49-Complete-row table deleted (positive assertions in verification, not Pending==0); the four-line forward-pointer blockquote sits where the table used to be.
5. `.planning/REQUIREMENTS.md` PASS-05 line is rewritten to match shipped precedence (BLOCKER 1).
6. All pre-existing tests still pass; no existing SPEC.md section was renumbered; no existing CLAUDE.md section was modified (only added to).
7. If `lychee` is present in the dev environment, it runs clean over SPEC.md — no broken internal or external links.
8. Cross-reference integrity: SPEC.md §3.5, CLAUDE.md convention, `.planning/REQUIREMENTS.md:7` Structure note, `.planning/REQUIREMENTS.md` PASS-05, and `tests/dht_label_constants.rs` failure messages all point at each other consistently.

**Note on WARNING 12:** Splitting 05-03 into three sub-plans (one per artifact) was considered and rejected — the execution overhead of three plans outweighs the risk reduction. 05-03 remains a single plan with 5 tasks (4 original + 1 new for BLOCKER 1).
</success_criteria>

<output>
After completion, create `.planning/phases/05-non-interactive-automation-e2e/05-03-SUMMARY.md` describing:
- SPEC.md changes by section (§7 precedence + strip-rule truth table, NEW §3.5 wire-stability subsection inserted between §3.4 and §4, §3/§4 version-prose rewrite, PKARR budget 550 B) — list each edit point and the line ranges affected. Note §3.5 is a NEW subsection, not an edit to existing §3.3.
- The new `tests/dht_label_constants.rs` — what it asserts, what a red test means (SPEC+code drift), §3.5 breadcrumb (BLOCKER 2 correction).
- CLAUDE.md addition — location (adjacent to GSD workflow), paragraph content (BLOCKER 3 rewrite — no "29 stale" narrative).
- Archive cleanup — 49-Complete-row table deleted (format-convention removal, not stale-data cleanup — BLOCKER 3 reframing); forward-pointer blockquote installed.
- `.planning/REQUIREMENTS.md` PASS-05 rewrite — old contradictory text replaced with shipped-precedence text (BLOCKER 1).
- Requirements covered: DOC-01 (version prose), DOC-02 (DHT label audit), DOC-03 (convention in CLAUDE.md), DOC-04 (archive cleanup) — four total. PASS-05 code enforcement remains on Plan 05-02; this plan only aligns the requirement TEXT.
- Note that this plan ran in parallel with Plan 05-01 (no code overlap confirmed by files_modified lists) and reached Wave 1 alongside it.
- Note that none of these changes block or unblock Plan 05-02 — the CLI work is independent of the documentation housekeeping.
</output>
